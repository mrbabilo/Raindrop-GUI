# Shell Tauri — Plan d'implémentation (phase 1, plan 3/3)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Faire de Raindrop-GUI une application macOS : une fenêtre qui
s'ouvre, un sidecar qu'elle lance et arrête elle-même, un token Bearer local
éphémère qu'elle génère, et le token Raindrop au trousseau — jusqu'au `.app`
et au `.dmg`.

**Architecture:** Le shell Rust est mince et ne parle jamais à Raindrop. Au
démarrage il résout `node`, lit le token Raindrop du trousseau macOS, génère
un token Bearer local, lance le sidecar avec les deux en variables
d'environnement, attend que le lockfile porte un port, puis répond au webview
qui lui demande son état. Le front décide alors ce qu'il affiche : l'écran de
premier lancement, l'écran de diagnostic, ou l'application. Le token Raindrop
ne traverse ni HTTP ni le front ; le token local n'est jamais écrit sur
disque.

**Tech Stack:** Tauri 2.11 (Rust 1.92, édition 2021), `keyring` 4 (trousseau
macOS par `apple-native-keyring-store/keychain`, activé par défaut),
`serde`/`serde_json`, le sidecar Node existant, le front React existant.

**Spec:** `docs/superpowers/specs/2026-09-15-raindrop-gui-design.md`
(§2 architecture, §3.2/§3.6/§3.7 corrections contraignantes, §6 token et
configuration, §7 états dégradés, §8 tests).

---

## Décisions d'architecture prises avant découpage

Ces trois points fanent sur plusieurs tasks. Ils sont tranchés ici, une fois,
avec leur raison — un exécutant ne les rouvre pas.

### D1 — Le webview reçoit sa connexion par une commande, qui remplit le global

`src/lib/connection.ts` lit `window.RAINDROP_GUI` **synchroniquement**, et
`request()` (api.ts) comme `jobEvents()` (sse.ts) l'appellent à chaque
requête. La spec §3.6/§3.7 dit que le token arrive « par commande Tauri »,
c'est-à-dire `invoke()`, donc **asynchrone**. Les deux ne composent pas
directement.

Un script d'initialisation (`initialization_script`, vérifié présent dans
tauri 2.11.5) ne suffit pas : son contenu est figé à la création de la
fenêtre, or au **premier lancement** aucun sidecar ne tourne encore — il n'y
a ni port ni token à injecter, et c'est justement l'écran de saisie du token
qu'il faut afficher.

**Décision :** `main.tsx` attend un amorçage avant de rendre. L'amorçage
appelle la commande, et **pose lui-même `window.RAINDROP_GUI`** quand l'état
est « prêt ». `connection.ts` n'est pas touché, `api.ts` et `sse.ts` non
plus, et ses trois tests passent inchangés. Le coût est un `await` unique
avant le premier rendu.

### D2 — Un sidecar d'un lancement précédent est terminé, pas réutilisé

`acquireLock` rend `"reused"` quand le lockfile porte un PID vivant, et le
sidecar sort alors en 0. Cette règle protège des **rechargements du webview**
(HMR), où le processus Tauri survit et ne relance rien.

Elle ne peut pas s'appliquer à Tauri au démarrage : le token Bearer local est
**généré à chaque lancement**. Un sidecar survivant d'un crash précédent
écoute avec l'ancien token — toutes les requêtes rendraient 401, sans qu'un
seul message n'explique pourquoi.

**Décision :** au démarrage, si le lockfile porte un PID vivant, Tauri le
**termine** (SIGTERM, puis SIGKILL après 3 s) et relance. C'est une précision
de la spec §3.6, pas une contradiction : la réutilisation qu'elle décrit vaut
entre sidecars, pas entre lancements de l'application.

### D3 — `node` est cherché, jamais supposé sur le PATH

**Mesuré le 2026-09-17 sur cette machine** : `node` est en
`/opt/homebrew/bin/node`, et le PATH que reçoit une application lancée depuis
le Finder est `/usr/gnu/bin:/usr/local/bin:/bin:/usr/bin:.` — `node` y est
**absent**. Un `Command::new("node")` marche en `tauri dev` et échoue dans
l'application livrée.

**Décision :** une résolution explicite (PATH hérité, puis candidats connus),
suivie d'un contrôle de version ≥ 20, alimente l'écran de diagnostic de la
spec §7 (« Node absent du PATH »). De même, `RAINDROP_MCP_ENTRY` est
**transmis explicitement** au spawn : son défaut est relatif au module du
sidecar et ne survit pas à l'empaquetage.

### Hors périmètre

Le **sélecteur du dossier de sauvegarde** (spec sauvegarde §4.3) que la
ROADMAP rattachait à ce plan en est **retiré** : sa spec attend encore la
relecture de l'utilisateur, et l'y inclure lierait ce plan à un document non
validé. Il fera son propre plan une fois la spec arrêtée.

---

## Global Constraints

Valeurs reprises **mot pour mot** de la spec §3 et de CLAUDE.md. Les exigences
de chaque task les incluent implicitement.

- `@kud/mcp-raindrop-io` est **épinglé à `1.3.1`** dans `package.json` —
  jamais de `npx @latest` au runtime ; spawn direct de
  `node_modules/@kud/mcp-raindrop-io/dist/index.js`.
- **Node ≥ 20 requis**, vérifié au démarrage ; à défaut, « écran de
  diagnostic au lancement avec instructions » (spec §7).
- Sidecar bindé **127.0.0.1 uniquement**, port 0.
- Token Bearer local **éphémère, généré par Tauri au lancement**, transmis au
  webview par commande Tauri, **jamais écrit en clair sur disque**.
- Le token Raindrop **ne transite jamais par HTTP** ni n'apparaît dans le
  front ; il est transmis au sidecar par `MCP_RAINDROPIO_TOKEN` au spawn.
- Le lockfile porte `{port, pid}` — **sans le token** (§3.7 prime sur §3.6).
- `Origin` vérifié côté sidecar : `tauri://localhost` (et `http://localhost:*`
  en dev), CORS minimal. Déjà implémenté (`sidecar/api/app.ts`) — ce plan ne
  doit pas le casser.
- Toute suppression passe par la **corbeille Raindrop** ; seul le vidage est
  définitif.
- **Pas d'E2E Tauri en Phase 1** (spec §8) : les tâches impures se vérifient à
  la main, avec un attendu écrit.
- **Taille des fichiers** (CLAUDE.md) : cible **≤ 300 lignes**, plafond dur
  **400** — y compris les fichiers Rust.
- Interface **en français**, textes externalisés dans `src/i18n/fr.ts`.
- Travailler sur `main`. **Ne pas pousser** sans demande explicite.
- Trailer de commit nommant le modèle réel, ici :
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

---

## Structure de fichiers

### Rust — `src-tauri/`

| Fichier | Responsabilité | Testé par |
|---|---|---|
| `Cargo.toml` | dépendances, nom de crate | — |
| `tauri.conf.json` | fenêtre, identifiant, bundle, ressources | — |
| `build.rs` | `tauri_build::build()` | — |
| `src/main.rs` | point d'entrée mince → `lib::run()` | — |
| `src/lib.rs` | assemblage : plugins, état, commandes, fenêtre | manuel |
| `src/jeton.rs` | génération du token Bearer local | `cargo test` |
| `src/node.rs` | résolution de `node` + contrôle de version | `cargo test` |
| `src/verrou.rs` | lecture du lockfile, décision de péremption | `cargo test` |
| `src/trousseau.rs` | token Raindrop au trousseau (`keyring`) | `cargo test` (ignoré par défaut) |
| `src/sidecar.rs` | spawn, attente du port, arrêt | manuel |
| `src/commandes.rs` | commandes exposées au webview | manuel |

Le partage est délibéré : **ce qui est pur est couvert par `cargo test`**
(token, résolution de node, lockfile, chemins), **ce qui touche le système
est mince et vérifié à la main** (fenêtre, spawn, commandes). La spec §8
interdit l'E2E Tauri en Phase 1 ; on ne fabrique donc pas un harnais qui
simulerait une fenêtre.

### Front — `src/`

| Fichier | Responsabilité |
|---|---|
| `lib/amorce.ts` | **créé** — appelle la commande, pose `window.RAINDROP_GUI` |
| `lib/amorce.test.ts` | **créé** — contrats de l'amorçage |
| `components/PremierLancement.tsx` | **créé** — saisie du token, validation, démarrage |
| `components/PremierLancement.test.tsx` | **créé** |
| `components/Diagnostic.tsx` | **créé** — Node absent, instructions |
| `components/Diagnostic.test.tsx` | **créé** |
| `main.tsx` | **modifié** — attend l'amorçage, choisit l'écran |
| `App.tsx` | **modifié** — `<Banners />` après le premier health `connected` |
| `i18n/fr.ts` | **modifié** — clés des deux écrans |
| `lib/connection.ts` | **inchangé** (décision D1) |

### Racine

| Fichier | Changement |
|---|---|
| `package.json` | scripts `tauri`, `tauri:dev`, `tauri:build` ; `@tauri-apps/cli` |
| `vite.config.ts` | `build.target` webview ; proxy déjà gardé (T1 ROADMAP) |
| `.gitignore` | `src-tauri/target/` |
| `docs/ROADMAP.md`, `CHANGELOG.md` | à jour en fin de plan |

---

## Vérifications faites avant d'écrire ce plan

Mesuré le 2026-09-17 sur la machine cible — l'exécutant n'a pas à les refaire,
mais saura quoi soupçonner si son résultat diffère.

| Question | Réponse mesurée |
|---|---|
| `tauri build` marche-t-il sans Xcode complet ? | **Oui.** Avec les seuls Command Line Tools : `.app` **et** `.dmg` produits, sortie 0. |
| La signature ? | **Ad-hoc** (`flags=0x20002(adhoc,linker-signed)`, `TeamIdentifier=not set`). Pas de Developer ID, donc Gatekeeper met l'app en quarantaine au premier lancement — voir Task 12. |
| `initialization_script` existe-t-il en Tauri 2 ? | Oui, `tauri-2.11.5/src/webview/mod.rs:868`. Non retenu (décision D1). |
| `node` est-il sur le PATH d'une app du Finder ? | **Non.** PATH minimal mesuré : `/usr/gnu/bin:/usr/local/bin:/bin:/usr/bin:.` ; `node` est en `/opt/homebrew/bin/node`. |
| `keyring` 4 marche-t-il sans réglage ? | Oui : la feature par défaut `v1` tire `apple-native-keyring-store/keychain`. |
| Que voit `beforeDevCommand` ? | `TAURI_ENV_PLATFORM`, `TAURI_ENV_ARCH`, `TAURI_ENV_FAMILY`, `TAURI_ENV_DEBUG`… — de quoi couper le proxy Vite. |
| Version des outils | `rustc 1.92.0`, `cargo 1.92.0`, `tauri-cli 2.11.4`, `tauri 2.11.5`, `node v26.8.2`. |

---

### Task 1: Squelette Tauri — la fenêtre s'ouvre, `cargo test` tourne

**Files:**
- Create: `src-tauri/Cargo.toml`
- Create: `src-tauri/tauri.conf.json`
- Create: `src-tauri/build.rs`
- Create: `src-tauri/src/main.rs`
- Create: `src-tauri/src/lib.rs`
- Create: `src-tauri/.gitignore`
- Create: `src-tauri/icons/` (générées)
- Modify: `package.json` (scripts + `@tauri-apps/cli`)
- Modify: `vite.config.ts` (couper le proxy sous Tauri)
- Modify: `.gitignore`

**Interfaces:**
- Consumes: rien (première task).
- Produces: `raindrop_gui_lib::run()`, appelée par `main.rs`. Le module
  `src-tauri/src/lib.rs` déclarera au fil des tasks : `mod jeton; mod node;
  mod verrou; mod trousseau; mod sidecar; mod commandes;`.

- [ ] **Step 1: Installer le CLI Tauri**

```bash
npm i -D @tauri-apps/cli@^2
npx tauri --version   # attendu : tauri-cli 2.11.x
```

- [ ] **Step 2: Écrire `src-tauri/Cargo.toml`**

`name = "raindrop-gui"` ; le crate lib s'appelle `raindrop_gui_lib` (tirets
interdits dans un nom de crate Rust).

```toml
[package]
name = "raindrop-gui"
version = "0.1.0"
description = "Raindrop GUI — navigation et nettoyage d'une bibliothèque Raindrop.io"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "raindrop_gui_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
keyring = "4"
```

Pas de crate d'aléa : le token local lit `/dev/urandom` (Task 2), ce qui
évite une dépendance pour trente octets.

- [ ] **Step 3: Écrire `src-tauri/build.rs`**

```rust
fn main() {
    tauri_build::build()
}
```

- [ ] **Step 4: Écrire `src-tauri/tauri.conf.json`**

`beforeDevCommand` lance Vite, `devUrl` pointe dessus ; en build, le front est
compilé puis servi depuis `../dist`.

```json
{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "productName": "Raindrop GUI",
  "version": "0.1.0",
  "identifier": "com.babilosapiens.raindrop-gui",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "npm run build:front",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "Raindrop GUI",
        "width": 1280,
        "height": 800,
        "minWidth": 960,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": { "csp": null }
  },
  "bundle": {
    "active": true,
    "targets": ["app", "dmg"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns"
    ]
  }
}
```

La largeur de 1280 n'est pas un chiffre rond posé au hasard : la grille du
front est `240px | 1fr | 320px` (`src/App.tsx`), soit 560 px de panneaux
fixes — 1280 laisse 720 px à la liste, et `minWidth: 960` en laisse 400,
sous quoi la liste devient illisible.

- [ ] **Step 5: Générer les icônes**

Une source carrée ≥ 1024 px est nécessaire. En son absence, produire un
carré uni provisoire et le noter en ROADMAP (l'icône définitive relève de
DESIGN.md, pas de ce plan) :

```bash
python3 - <<'PY'
import struct, zlib, io
# PNG 1024×1024 uni, couleur de marque provisoire (#2F6FEB)
w = h = 1024
px = b''.join(b'\x00' + bytes([0x2F, 0x6F, 0xEB]) * w for _ in range(h))
def chunk(t, d):
    c = t + d
    return struct.pack('>I', len(d)) + c + struct.pack('>I', zlib.crc32(c) & 0xffffffff)
png = (b'\x89PNG\r\n\x1a\n'
       + chunk(b'IHDR', struct.pack('>IIBBBBB', w, h, 8, 2, 0, 0, 0))
       + chunk(b'IDAT', zlib.compress(px, 9))
       + chunk(b'IEND', b''))
open('/tmp/raindrop-icone.png', 'wb').write(png)
print('écrit /tmp/raindrop-icone.png')
PY
npx tauri icon /tmp/raindrop-icone.png
```

- [ ] **Step 6: Écrire `src-tauri/src/main.rs`**

```rust
// Le binaire ne fait qu'appeler la bibliothèque : tout le code vit dans
// lib.rs, seul endroit que `cargo test` sait exercer.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    raindrop_gui_lib::run()
}
```

- [ ] **Step 7: Écrire `src-tauri/src/lib.rs` (squelette + harnais de test)**

```rust
pub fn run() {
    tauri::Builder::default()
        .run(tauri::generate_context!())
        .expect("erreur au lancement de l'application Tauri");
}

#[cfg(test)]
mod tests {
    // Harnais : les tasks suivantes déposent ici leurs modules purs. Ce
    // premier test ne prouve qu'une chose — `cargo test` compile et tourne
    // sur ce crate — mais c'est la marche sur laquelle tout le reste monte.
    #[test]
    fn le_harnais_de_test_tourne() {
        assert_eq!(2 + 2, 4);
    }
}
```

- [ ] **Step 8: Écrire `src-tauri/.gitignore` et compléter celui de la racine**

`src-tauri/.gitignore` :

```
/target
/gen
```

Vérifier que la racine ignore bien les artefacts :

```bash
grep -q "src-tauri/target" .gitignore || echo "src-tauri/target/" >> .gitignore
```

- [ ] **Step 9: Ajouter les scripts npm**

```bash
python3 - <<'PY'
import json, io
d = json.load(open('package.json'))
d['scripts']['tauri'] = 'tauri'
d['scripts']['tauri:dev'] = 'tauri dev'
d['scripts']['tauri:build'] = 'tauri build'
io.open('package.json', 'w', encoding='utf-8').write(json.dumps(d, indent=2, ensure_ascii=False) + '\n')
print('scripts ajoutés')
PY
```

- [ ] **Step 10: Couper le proxy Vite sous Tauri**

Sans cela `tauri dev` **se bloque** : `beforeDevCommand` lance Vite, dont la
config appelle `sidecarPort()`, qui attend 10 s un lockfile qu'aucun sidecar
n'a écrit — le sidecar étant lancé par le code Rust, qui ne démarre qu'après.
Sous Tauri le proxy est de toute façon inutile : le front tape directement
`http://127.0.0.1:<port>` via `window.RAINDROP_GUI`.

Dans `vite.config.ts`, remplacer la condition du bloc `server` :

```ts
  server:
    command === "serve" &&
    !isPreview &&
    process.env.VITEST === undefined &&
    // Sous Tauri (`tauri dev` pose TAURI_ENV_PLATFORM avant d'appeler cette
    // commande), le front parle DIRECTEMENT au sidecar par
    // window.RAINDROP_GUI : le proxy n'a rien à faire, et sidecarPort()
    // bloquerait 10 s sur un lockfile que Tauri n'a pas encore écrit.
    process.env.TAURI_ENV_PLATFORM === undefined
      ? {
          port: 5173,
          proxy: {
            "/api": {
              target: `http://127.0.0.1:${sidecarPort()}`,
              changeOrigin: false,
            },
          },
        }
      : { port: 5173, strictPort: true },
```

- [ ] **Step 11: Lancer `cargo test`**

```bash
cd src-tauri && cargo test 2>&1 | tail -5
```

Attendu : `test tests::le_harnais_de_test_tourne ... ok`, `test result: ok. 1
passed`. La première compilation prend plusieurs minutes (≈ 400 crates).

- [ ] **Step 12: Vérification manuelle — la fenêtre s'ouvre**

```bash
npm run tauri:dev
```

Attendu : une fenêtre « Raindrop GUI » de 1280×800 s'ouvre et affiche
l'interface React (barre latérale, liste, détail). La liste affichera une
erreur de chargement — c'est **normal** : aucun sidecar ne tourne encore,
c'est l'objet des tasks 6 et 7. Quitter avec ⌘Q.

- [ ] **Step 12 bis : LE contrôle qui décide du reste — le webview
      atteint-il 127.0.0.1 ?**

**À ne pas sauter.** Sous Tauri l'origine de la page est `tauri://localhost`
et le front va chercher `http://127.0.0.1:<port>` : une requête d'un schéma
personnalisé vers du HTTP en clair. **Ce chemin n'a jamais été exercé** — les
plans 1 et 2 sont passés par le proxy Vite, en même origine. Si WKWebView le
refuse, la décision D1 et toute la moitié front de ce plan tombent, et tout
doit passer par `invoke`. `csp: null` lève un obstacle, il ne tranche pas la
question.

Dans un terminal, lancer un sidecar de dev et lire son port :

```bash
./scripts/dev-sidecar.sh &
sleep 5
python3 -c "import json,os;print(json.load(open(os.path.expanduser('~/Library/Application Support/Raindrop-GUI/sidecar.json')))['port'])"
```

Puis, dans l'inspecteur de la fenêtre Tauri (clic droit → Inspect Element), en
remplaçant `<PORT>` :

```js
await (await fetch("http://127.0.0.1:<PORT>/api/health",
  { headers: { Authorization: "Bearer dev-local-token" } })).json()
```

Attendu : `{status: "ok", mcp: "connected"}`.

**Si l'appel échoue** (erreur CORS, schéma refusé, requête bloquée) :
**arrêter le plan ici** et le reprendre — tout l'accès aux données devra
passer par des commandes Tauri, ce qui change `api.ts`, `sse.ts` et la
décision D1. Le découvrir maintenant coûte une heure ; le découvrir à la
Task 7 coûte sept tasks.

Couper le sidecar ensuite : `pkill -f "tsx watch sidecar/index.ts"`.

- [ ] **Step 13: Vérifier que rien de lourd n'est suivi par git**

```bash
git status --short | grep -E "target/|\.dmg|\.app" && echo "FUITE" || echo "propre"
```

Attendu : `propre`.

- [ ] **Step 14: Commit**

```bash
git add src-tauri package.json package-lock.json vite.config.ts .gitignore
git commit -F - <<'EOF'
feat(tauri): squelette du shell — la fenêtre s'ouvre

Plan 3/3, Task 1. Rien ne fonctionne encore côté données : la fenêtre
charge le front, qui affiche son erreur de chargement faute de sidecar.
C'est la marche, pas la maison.

Deux pièges désamorcés ici plutôt que découverts plus tard :
- le proxy Vite est coupé sous Tauri (TAURI_ENV_PLATFORM) — sinon
  `tauri dev` se bloque dix secondes sur un lockfile que personne n'a
  écrit, le sidecar étant lancé par le code Rust qui n'a pas démarré ;
- `src-tauri/target/` est ignoré avant le premier build, qui pèse ~2 Go.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 2: Le token Bearer local

**Files:**
- Create: `src-tauri/src/jeton.rs`
- Modify: `src-tauri/src/lib.rs` (déclarer `mod jeton;`)

**Interfaces:**
- Consumes: Task 1 (`cargo test` tourne).
- Produces: `jeton::engendrer() -> std::io::Result<String>` (64 caractères
  hexadécimaux) et `jeton::en_hex(&[u8]) -> String`. Consommé par Task 6
  (variable d'environnement du sidecar) et Task 7 (réponse au webview).

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `src-tauri/src/jeton.rs` :

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn hex_rend_deux_caracteres_par_octet_zero_compris() {
        // Le piège classique : un octet < 16 rendu sur un seul caractère
        // raccourcit le token sans que rien ne le signale.
        assert_eq!(en_hex(&[0x00, 0x0f, 0xa0, 0xff]), "000fa0ff");
    }

    #[test]
    fn le_token_fait_soixante_quatre_caracteres_hexadecimaux() {
        let t = engendrer().expect("aléa système lisible");
        assert_eq!(t.len(), 64, "32 octets → 64 caractères");
        assert!(t.chars().all(|c| c.is_ascii_hexdigit()));
    }

    #[test]
    fn deux_tokens_different() {
        // Éphémère veut dire « un par lancement » : un token constant
        // rendrait la superficie HTTP locale permanente (spec §3.7).
        assert_ne!(engendrer().unwrap(), engendrer().unwrap());
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
cd src-tauri && cargo test jeton 2>&1 | tail -15
```

Attendu : ÉCHEC de compilation — `cannot find function 'en_hex' in this scope`,
`cannot find function 'engendrer' in this scope`.

- [ ] **Step 3: Écrire l'implémentation**

En tête de `src-tauri/src/jeton.rs`, avant le `mod tests` :

```rust
//! Token Bearer local, éphémère (spec §3.7).
//!
//! Il n'est JAMAIS écrit sur disque : il ne vit qu'en mémoire du processus
//! Tauri, dans l'environnement du sidecar qu'il lance, et dans le webview
//! auquel il le transmet par commande. Le lockfile ne le porte pas non plus
//! (`sidecar/lockfile.ts`, ruling R15).

use std::fmt::Write as _;
use std::fs::File;
use std::io::Read;

/// 32 octets : bien au-delà de ce qu'une superficie locale exige, et le coût
/// est nul.
const OCTETS: usize = 32;

/// Rend des octets en hexadécimal minuscule, deux caractères par octet.
pub fn en_hex(octets: &[u8]) -> String {
    let mut s = String::with_capacity(octets.len() * 2);
    for o in octets {
        // Écrire dans une String ne peut pas échouer.
        let _ = write!(s, "{o:02x}");
    }
    s
}

/// Engendre le token du lancement. `/dev/urandom` plutôt qu'un crate d'aléa :
/// une dépendance de moins pour trente octets, et la source est celle que le
/// système offre de toute façon.
pub fn engendrer() -> std::io::Result<String> {
    let mut buf = [0u8; OCTETS];
    File::open("/dev/urandom")?.read_exact(&mut buf)?;
    Ok(en_hex(&buf))
}
```

Et dans `src-tauri/src/lib.rs`, en tête :

```rust
mod jeton;
```

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

```bash
cd src-tauri && cargo test jeton 2>&1 | tail -8
```

Attendu : `test result: ok. 3 passed`.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/jeton.rs src-tauri/src/lib.rs
git commit -F - <<'EOF'
feat(tauri): token Bearer local éphémère

Plan 3/3, Task 2. 32 octets de /dev/urandom en hexadécimal, un par
lancement. Jamais sur disque (spec §3.7) : il ne vit qu'en mémoire du
processus Tauri, dans l'environnement du sidecar et dans le webview.

/dev/urandom plutôt qu'un crate d'aléa — une dépendance de moins pour
trente octets, et c'est la source que le système offre de toute façon.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 3: Résoudre `node` sans se fier au PATH

**Files:**
- Create: `src-tauri/src/node.rs`
- Modify: `src-tauri/src/lib.rs` (déclarer `mod node;`)

**Interfaces:**
- Consumes: Task 1.
- Produces:
  - `node::Verdict` — `Trouve { chemin: PathBuf, version: String }`,
    `TropVieux { chemin: PathBuf, version: String }`, `Absent`.
  - `node::resoudre() -> Verdict` (impur : sonde le système).
  - `node::choisir(&[PathBuf], impl Fn(&Path) -> Option<String>) -> Verdict` (pur).
  - `node::majeure(&str) -> Option<u32>`.
  Consommé par Task 6 (chemin du binaire au spawn) et Task 7 (état
  `node_absent` rendu au webview).

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `src-tauri/src/node.rs` :

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    fn p(s: &str) -> PathBuf {
        PathBuf::from(s)
    }

    #[test]
    fn la_majeure_se_lit_avec_ou_sans_v() {
        assert_eq!(majeure("v20.11.1"), Some(20));
        assert_eq!(majeure("22.3.0\n"), Some(22));
        assert_eq!(majeure("  v26.8.2  "), Some(26));
        assert_eq!(majeure("pas une version"), None);
        assert_eq!(majeure(""), None);
    }

    #[test]
    fn le_premier_candidat_conforme_gagne() {
        let candidats = vec![p("/absent/node"), p("/opt/homebrew/bin/node")];
        let v = choisir(&candidats, |c| {
            (c == p("/opt/homebrew/bin/node").as_path()).then(|| "v26.8.2".to_string())
        });
        assert_eq!(
            v,
            Verdict::Trouve { chemin: p("/opt/homebrew/bin/node"), version: "v26.8.2".into() }
        );
    }

    // Le cas qui motive toute cette task : mesuré le 2026-09-17, une app
    // lancée du Finder reçoit un PATH où node est ABSENT, alors qu'il est
    // installé en /opt/homebrew/bin. Sonder les candidats connus est la
    // seule façon de ne pas échouer là où l'utilisateur voit node marcher.
    #[test]
    fn aucun_candidat_ne_repond_absent() {
        assert_eq!(choisir(&[p("/a"), p("/b")], |_| None), Verdict::Absent);
    }

    #[test]
    fn un_node_trop_vieux_est_distingue_d_une_absence() {
        // Spec §3.2 : Node >= 20. Dire « absent » quand il est seulement
        // trop vieux enverrait l'utilisateur installer ce qu'il a déjà.
        let v = choisir(&[p("/usr/bin/node")], |_| Some("v18.19.0".to_string()));
        assert_eq!(
            v,
            Verdict::TropVieux { chemin: p("/usr/bin/node"), version: "v18.19.0".into() }
        );
    }

    #[test]
    fn un_conforme_plus_loin_bat_un_trop_vieux_plus_tot() {
        let candidats = vec![p("/usr/bin/node"), p("/opt/homebrew/bin/node")];
        let v = choisir(&candidats, |c| {
            Some(if c == p("/usr/bin/node").as_path() { "v18.0.0" } else { "v20.0.0" }.to_string())
        });
        assert_eq!(
            v,
            Verdict::Trouve { chemin: p("/opt/homebrew/bin/node"), version: "v20.0.0".into() }
        );
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
cd src-tauri && cargo test node 2>&1 | tail -15
```

Attendu : ÉCHEC de compilation — `cannot find function 'majeure'`,
`cannot find function 'choisir'`, `cannot find type 'Verdict'`.

- [ ] **Step 3: Écrire l'implémentation**

En tête de `src-tauri/src/node.rs` :

```rust
//! Résolution de l'interpréteur Node (spec §3.2, §7).
//!
//! MESURÉ le 2026-09-17 : une application lancée depuis le Finder reçoit
//! `/usr/gnu/bin:/usr/local/bin:/bin:/usr/bin:.`, où node est ABSENT — il est
//! installé en `/opt/homebrew/bin/node`. Un `Command::new("node")` marche donc
//! en `tauri dev` (qui hérite du PATH du terminal) et échoue dans
//! l'application livrée. On sonde, on ne suppose pas.

use std::path::{Path, PathBuf};
use std::process::Command;

/// Version minimale exigée par le sidecar et le serveur MCP (spec §3.2).
pub const MAJEURE_MINIMALE: u32 = 20;

/// Emplacements sondés en plus du PATH hérité, dans cet ordre.
pub const CANDIDATS: &[&str] = &[
    "/opt/homebrew/bin/node", // Homebrew, Apple Silicon
    "/usr/local/bin/node",    // Homebrew, Intel — et installeur officiel
    "/usr/bin/node",          // système, rare sur macOS
];

#[derive(Debug, PartialEq, Eq)]
pub enum Verdict {
    Trouve { chemin: PathBuf, version: String },
    /// Présent mais sous le plancher : le dire évite d'envoyer l'utilisateur
    /// installer ce qu'il a déjà.
    TropVieux { chemin: PathBuf, version: String },
    Absent,
}

/// Majeure d'une sortie `node --version` (« v20.11.1 » → 20).
pub fn majeure(sortie: &str) -> Option<u32> {
    sortie
        .trim()
        .trim_start_matches('v')
        .split('.')
        .next()?
        .parse()
        .ok()
}

/// Choisit parmi des candidats, `sonder` rendant la version annoncée par un
/// chemin (ou `None` s'il n'est pas exécutable). Pur : c'est ici que vivent
/// les règles, la sonde système est injectée.
pub fn choisir<F>(candidats: &[PathBuf], sonder: F) -> Verdict
where
    F: Fn(&Path) -> Option<String>,
{
    let mut trop_vieux: Option<(PathBuf, String)> = None;
    for c in candidats {
        let Some(version) = sonder(c) else { continue };
        let Some(m) = majeure(&version) else { continue };
        if m >= MAJEURE_MINIMALE {
            return Verdict::Trouve { chemin: c.clone(), version };
        }
        // On garde le premier trop vieux, mais on continue : un node
        // conforme plus loin dans la liste doit l'emporter.
        if trop_vieux.is_none() {
            trop_vieux = Some((c.clone(), version));
        }
    }
    match trop_vieux {
        Some((chemin, version)) => Verdict::TropVieux { chemin, version },
        None => Verdict::Absent,
    }
}

/// Les candidats réels : d'abord le PATH hérité (le cas du développeur, où
/// `node` est celui qu'il utilise), puis les emplacements connus.
pub fn candidats_systeme() -> Vec<PathBuf> {
    let mut v = Vec::new();
    if let Ok(path) = std::env::var("PATH") {
        for d in std::env::split_paths(&path) {
            v.push(d.join("node"));
        }
    }
    for c in CANDIDATS {
        let c = PathBuf::from(c);
        if !v.contains(&c) {
            v.push(c);
        }
    }
    v
}

/// Sonde le système. Impur — la logique est dans `choisir`.
pub fn resoudre() -> Verdict {
    choisir(&candidats_systeme(), |chemin| {
        let sortie = Command::new(chemin).arg("--version").output().ok()?;
        sortie
            .status
            .success()
            .then(|| String::from_utf8_lossy(&sortie.stdout).trim().to_string())
    })
}
```

Et dans `src-tauri/src/lib.rs` :

```rust
mod node;
```

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

```bash
cd src-tauri && cargo test node 2>&1 | tail -8
```

Attendu : `test result: ok. 5 passed`.

- [ ] **Step 5: Vérifier la résolution réelle**

Ajouter temporairement un test qui imprime le verdict, et l'exécuter :

```bash
cd src-tauri && cat >> src/node.rs <<'RS'

#[cfg(test)]
mod sonde_reelle {
    #[test]
    #[ignore = "sonde la machine : `cargo test -- --ignored` pour l'exécuter"]
    fn ou_est_node_sur_cette_machine() {
        println!("{:?}", super::resoudre());
    }
}
RS
cargo test ou_est_node -- --ignored --nocapture 2>&1 | grep -A2 "ou_est_node"
```

Attendu sur cette machine : `Trouve { chemin: "/opt/homebrew/bin/node",
version: "v26.8.2" }`. Ce test reste dans le dépôt, marqué `#[ignore]` : il
ne tourne pas en routine mais répond en une commande à « pourquoi ne trouve-t-il
pas node ? ».

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/node.rs src-tauri/src/lib.rs
git commit -F - <<'EOF'
feat(tauri): résoudre node au lieu de le supposer sur le PATH

Plan 3/3, Task 3. MESURÉ : une app lancée du Finder reçoit
/usr/gnu/bin:/usr/local/bin:/bin:/usr/bin:. — node est en
/opt/homebrew/bin, donc ABSENT de ce PATH. Command::new("node") aurait
marché en `tauri dev` (qui hérite du PATH du terminal) et échoué dans
l'application livrée : le pire des deux mondes, un bug qui ne se montre
qu'après empaquetage.

On sonde donc le PATH hérité puis les emplacements connus, et on
distingue « trop vieux » d'« absent » — dire « absent » d'un node 18
enverrait l'utilisateur installer ce qu'il a déjà.

La logique est pure et testée ; la sonde système est injectée.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 4: Le lockfile — lire, et décider du sort d'un sidecar survivant

**Files:**
- Create: `src-tauri/src/verrou.rs`
- Modify: `src-tauri/src/lib.rs` (déclarer `mod verrou;`)

**Interfaces:**
- Consumes: Task 1.
- Produces:
  - `verrou::Verrou { port: u16, pid: i32 }` (désérialisé de `sidecar.json`).
  - `verrou::lire(&str) -> Option<Verrou>`.
  - `verrou::decider(Option<&Verrou>, impl Fn(i32) -> bool) -> Decision`
    avec `Decision::{Aucun, Terminer(i32)}`.
  - `verrou::port_pret(&Verrou) -> Option<u16>`.
  - `verrou::dossier_donnees() -> PathBuf`, `verrou::chemin(&Path) -> PathBuf`.
  Consommé par Task 6.

**Contexte indispensable :** le sidecar écrit
`~/Library/Application Support/Raindrop-GUI/sidecar.json` avec
`{port, pid, startedAt}` — **sans le token** (`sidecar/lockfile.ts`, ruling
R15). `port: 0` signifie « binding en cours » (spec §3.6). La décision D2 en
tête de ce plan explique pourquoi Tauri **termine** un sidecar survivant au
lieu de le réutiliser.

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `src-tauri/src/verrou.rs` :

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn lit_le_lockfile_que_le_sidecar_ecrit_vraiment() {
        // Forme exacte de sidecar/lockfile.ts : startedAt en plus, et
        // AUCUN token (ruling R15) — le champ inconnu doit être ignoré,
        // pas faire échouer la lecture.
        let brut = r#"{"port":51234,"pid":4242,"startedAt":"2026-09-17T10:00:00.000Z"}"#;
        assert_eq!(lire(brut), Some(Verrou { port: 51234, pid: 4242 }));
    }

    #[test]
    fn un_lockfile_illisible_ne_fait_pas_tomber_le_demarrage() {
        // Spec §7, « port/lockfile corrompu » : on repart, on ne plante pas.
        assert_eq!(lire("{ pas du json"), None);
        assert_eq!(lire(""), None);
        assert_eq!(lire(r#"{"pid":1}"#), None); // port manquant
    }

    #[test]
    fn port_zero_veut_dire_binding_en_cours() {
        // Spec §3.6 : port 0 = le sidecar n'a pas encore son port.
        assert_eq!(port_pret(&Verrou { port: 0, pid: 1 }), None);
        assert_eq!(port_pret(&Verrou { port: 51234, pid: 1 }), Some(51234));
    }

    #[test]
    fn sans_lockfile_on_lance_simplement() {
        assert_eq!(decider(None, |_| true), Decision::Aucun);
    }

    #[test]
    fn un_pid_mort_ne_demande_rien() {
        // Lockfile resté d'un arrêt brutal : le sidecar l'écrasera.
        let v = Verrou { port: 51234, pid: 4242 };
        assert_eq!(decider(Some(&v), |_| false), Decision::Aucun);
    }

    // Le cœur de la décision D2 : un sidecar VIVANT d'un lancement précédent
    // écoute avec l'ANCIEN token local. Le réutiliser ferait répondre 401 à
    // toutes les requêtes, sans qu'un seul message ne l'explique.
    #[test]
    fn un_sidecar_survivant_est_termine_pas_reutilise() {
        let v = Verrou { port: 51234, pid: 4242 };
        assert_eq!(decider(Some(&v), |pid| pid == 4242), Decision::Terminer(4242));
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
cd src-tauri && cargo test verrou 2>&1 | tail -15
```

Attendu : ÉCHEC de compilation — `cannot find type 'Verrou'`,
`cannot find function 'lire'`, `cannot find function 'decider'`.

- [ ] **Step 3: Écrire l'implémentation**

En tête de `src-tauri/src/verrou.rs` :

```rust
//! Le lockfile du sidecar, vu depuis Tauri.
//!
//! Écrit par `sidecar/lockfile.ts` dans le dossier app-data :
//! `{port, pid, startedAt}` — **jamais le token** (§3.7 prime sur §3.6).
//! `port: 0` = binding en cours.

use serde::Deserialize;
use std::path::{Path, PathBuf};

/// Le nom du fichier est fixé par `sidecar/lockfile.ts`. S'il change là-bas,
/// il doit changer ici : les deux moitiés se rencontrent sur ce nom.
const FICHIER: &str = "sidecar.json";

#[derive(Debug, Deserialize, PartialEq, Eq)]
pub struct Verrou {
    pub port: u16,
    pub pid: i32,
}

#[derive(Debug, PartialEq, Eq)]
pub enum Decision {
    /// Rien à faire avant de lancer.
    Aucun,
    /// Un sidecar d'un lancement précédent est vivant : le terminer (D2).
    Terminer(i32),
}

/// Lit un lockfile. Tout échec rend `None` : un fichier corrompu fait
/// repartir de zéro, il ne fait pas tomber le démarrage (spec §7).
pub fn lire(texte: &str) -> Option<Verrou> {
    serde_json::from_str(texte).ok()
}

/// `port: 0` = binding en cours (spec §3.6) : le port n'est pas encore connu.
pub fn port_pret(v: &Verrou) -> Option<u16> {
    (v.port > 0).then_some(v.port)
}

/// Que faire du lockfile trouvé au démarrage. `vivant` est injecté (le test
/// n'a pas à fabriquer de vrais processus).
pub fn decider<F>(verrou: Option<&Verrou>, vivant: F) -> Decision
where
    F: Fn(i32) -> bool,
{
    match verrou {
        Some(v) if vivant(v.pid) => Decision::Terminer(v.pid),
        _ => Decision::Aucun,
    }
}

/// Le MÊME dossier que `sidecar/config.ts::appDataDir` — c'est là que le
/// lockfile s'écrit et se lit. Les deux doivent s'accorder ou rien ne se
/// trouve.
pub fn dossier_donnees() -> PathBuf {
    let home = std::env::var("HOME").unwrap_or_default();
    PathBuf::from(home).join("Library/Application Support/Raindrop-GUI")
}

pub fn chemin(dossier: &Path) -> PathBuf {
    dossier.join(FICHIER)
}
```

Et dans `src-tauri/src/lib.rs` :

```rust
mod verrou;
```

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

```bash
cd src-tauri && cargo test verrou 2>&1 | tail -8
```

Attendu : `test result: ok. 6 passed`.

- [ ] **Step 5: Vérifier l'accord avec le sidecar réel**

Le lockfile lu ici est écrit là-bas : contrôler que les deux nomment le même
fichier et le même dossier.

```bash
cd /Users/davidbaudoin/Documents/GitHub/Raindrop-GUI
grep -n 'fileName\|"Application Support"' sidecar/lockfile.ts sidecar/config.ts
grep -n 'FICHIER\|Application Support' src-tauri/src/verrou.rs
```

Attendu : `sidecar.json` et `Library/Application Support/Raindrop-GUI` des
deux côtés.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/verrou.rs src-tauri/src/lib.rs
git commit -F - <<'EOF'
feat(tauri): lire le lockfile, et terminer un sidecar survivant

Plan 3/3, Task 4. Le lockfile porte {port, pid} — jamais le token (R15).
port: 0 veut dire « binding en cours » (spec §3.6), pas « pas de port ».

La décision qui compte : un sidecar VIVANT d'un lancement précédent n'est
pas réutilisé, il est terminé. Le token Bearer local étant régénéré à
chaque lancement, le réutiliser ferait répondre 401 à toutes les requêtes
sans qu'un seul message ne l'explique. La réutilisation que décrit la
spec §3.6 vaut entre sidecars (un second exemplaire sort en 0), pas entre
lancements de l'application.

Un lockfile corrompu rend None et fait repartir de zéro (spec §7).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 5: Le token Raindrop au trousseau macOS

**Files:**
- Create: `src-tauri/src/trousseau.rs`
- Modify: `src-tauri/src/lib.rs` (déclarer `mod trousseau;`)

**Interfaces:**
- Consumes: Task 1.
- Produces:
  - `trousseau::lire() -> Result<Option<String>, String>` — `Ok(None)` quand
    aucun token n'est enregistré (premier lancement), `Err` sur panne réelle.
  - `trousseau::ecrire(&str) -> Result<(), String>`.
  - `trousseau::effacer() -> Result<(), String>`.
  - `trousseau::SERVICE` / `trousseau::compte()`.
  Consommé par Task 6 (spawn) et Task 7 (commandes).

**Contexte :** la spec §6 dit « trousseau macOS via plugin Tauri (Keychain) ».
Il n'existe pas de plugin Tauri officiel pour le trousseau ; la voie réelle
est le crate `keyring` (4.2.0, 25 M téléchargements), dont la feature par
défaut `v1` tire `apple-native-keyring-store/keychain`. **C'est une précision
de vocabulaire de la spec, pas un changement de comportement** : le secret va
bien au trousseau macOS.

Le service et le compte doivent être **ceux du script de dev**
(`scripts/dev-sidecar.sh` : service `raindrop-api-token`, compte `$USER`) pour
que développement et application partagent le même enregistrement — sinon
l'utilisateur saisirait son token deux fois, et une rotation n'en changerait
qu'un.

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `src-tauri/src/trousseau.rs`. Les tests qui touchent vraiment le
trousseau sont `#[ignore]` : ils font apparaître une demande d'autorisation
macOS et modifient un secret réel — ils n'ont rien à faire dans une suite de
routine (spec §8 : aucun test ne touche de ressource réelle sans opt-in).

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn le_service_est_celui_du_script_de_dev() {
        // scripts/dev-sidecar.sh : KEYCHAIN_SERVICE="raindrop-api-token".
        // S'ils divergent, l'utilisateur saisit son token deux fois et une
        // rotation n'en change qu'un.
        assert_eq!(SERVICE, "raindrop-api-token");
    }

    // Test NON tautologique : comparer compte() à std::env::var("USER") ne
    // prouverait rien (toujours vrai dans un terminal) et tairait le cas qui
    // compte — une application lancée par launchd peut n'avoir AUCUN USER.
    // Sans repli, compte() rendrait "", l'entrée serait introuvable, et le
    // symptôme serait l'écran de premier lancement à CHAQUE lancement alors
    // que le jeton est bien au trousseau.
    #[test]
    fn le_compte_se_replie_sur_le_dossier_personnel() {
        assert_eq!(compte_depuis(Some("bob"), Some("/Users/alice")), "bob");
        assert_eq!(compte_depuis(None, Some("/Users/alice")), "alice");
        assert_eq!(compte_depuis(Some(""), Some("/Users/alice")), "alice");
        assert_eq!(compte_depuis(None, None), "");
    }

    #[test]
    fn un_compte_vide_est_refuse_au_lieu_d_etre_cherche() {
        // Chercher au trousseau avec un compte vide rendrait « absent » —
        // indiscernable d'un premier lancement. On veut une panne lisible.
        assert!(verifier_compte("").is_err());
        assert!(verifier_compte("alice").is_ok());
    }

    #[test]
    #[ignore = "touche le vrai trousseau : `cargo test -- --ignored`"]
    fn aller_retour_ecrire_lire_effacer() {
        let temoin = "jeton-de-test-ne-pas-utiliser";
        let avant = lire().expect("lecture possible");
        ecrire(temoin).expect("écriture possible");
        assert_eq!(lire().unwrap().as_deref(), Some(temoin));
        effacer().expect("effacement possible");
        assert_eq!(lire().unwrap(), None, "après effacement : absent, pas erreur");
        // Restaurer ce qui s'y trouvait, pour ne pas casser le dev.
        if let Some(t) = avant {
            ecrire(&t).expect("restauration");
        }
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
cd src-tauri && cargo test trousseau 2>&1 | tail -15
```

Attendu : ÉCHEC de compilation — `cannot find value 'SERVICE'`,
`cannot find function 'compte'`.

- [ ] **Step 3: Écrire l'implémentation**

En tête de `src-tauri/src/trousseau.rs` :

```rust
//! Le token Raindrop, au trousseau macOS (spec §6).
//!
//! La spec dit « plugin Tauri (Keychain) » ; il n'existe pas de plugin
//! officiel — la voie réelle est le crate `keyring`, dont la feature par
//! défaut `v1` tire `apple-native-keyring-store/keychain`. Le secret va bien
//! au trousseau macOS : c'est le vocabulaire qui est corrigé, pas le
//! comportement.
//!
//! Service et compte sont CEUX DU SCRIPT DE DEV (`scripts/dev-sidecar.sh`)
//! pour que les deux chemins partagent un seul enregistrement.

use keyring::{Entry, Error};

/// Doit rester identique à KEYCHAIN_SERVICE de scripts/dev-sidecar.sh.
pub const SERVICE: &str = "raindrop-api-token";

/// Le script utilise `-a "$USER"` : même convention, avec un repli.
///
/// Une application lancée par launchd (donc depuis le Finder) peut n'avoir
/// aucun `USER` dans son environnement. Sans repli, le compte serait vide,
/// l'entrée introuvable, et le symptôme serait l'écran de premier lancement
/// à CHAQUE ouverture — alors que le jeton est bien au trousseau.
pub fn compte() -> String {
    compte_depuis(
        std::env::var("USER").ok().as_deref(),
        std::env::var("HOME").ok().as_deref(),
    )
}

/// Pur, pour être testable sans toucher à l'environnement du test.
pub fn compte_depuis(user: Option<&str>, home: Option<&str>) -> String {
    if let Some(u) = user {
        if !u.is_empty() {
            return u.to_string();
        }
    }
    home.map(std::path::Path::new)
        .and_then(std::path::Path::file_name)
        .map(|s| s.to_string_lossy().into_owned())
        .unwrap_or_default()
}

/// Un compte vide chercherait au trousseau et rendrait « absent » —
/// indiscernable d'un premier lancement. On veut une panne qui se lit.
pub fn verifier_compte(c: &str) -> Result<(), String> {
    if c.is_empty() {
        return Err("compte utilisateur indéterminable (ni USER ni HOME)".into());
    }
    Ok(())
}

fn entree() -> Result<Entry, String> {
    let c = compte();
    verifier_compte(&c)?;
    Entry::new(SERVICE, &c).map_err(|e| format!("trousseau inaccessible : {e}"))
}

/// `Ok(None)` = aucun token enregistré (premier lancement, spec §6).
/// `Err` = panne réelle du trousseau — à ne pas confondre avec l'absence,
/// qui est un état normal et attendu.
pub fn lire() -> Result<Option<String>, String> {
    match entree()?.get_password() {
        Ok(t) => Ok(Some(t)),
        Err(Error::NoEntry) => Ok(None),
        Err(e) => Err(format!("lecture du trousseau impossible : {e}")),
    }
}

pub fn ecrire(token: &str) -> Result<(), String> {
    entree()?
        .set_password(token)
        .map_err(|e| format!("écriture au trousseau impossible : {e}"))
}

/// Effacer un token absent n'est pas une erreur : le résultat voulu est
/// « plus de token », et il est atteint.
pub fn effacer() -> Result<(), String> {
    match entree()?.delete_credential() {
        Ok(()) | Err(Error::NoEntry) => Ok(()),
        Err(e) => Err(format!("effacement du trousseau impossible : {e}")),
    }
}
```

Et dans `src-tauri/src/lib.rs` :

```rust
mod trousseau;
```

**Note pour l'exécutant :** si `Error::NoEntry` n'existe pas sous ce nom dans
la version tirée, lister les variantes avant d'inventer —
`cargo doc -p keyring --open`, ou `grep -rn "pub enum Error" -A 30
~/.cargo/registry/src/*/keyring-4*/src/`. La variante recherchée est celle
qui signifie « entrée absente » (`NoEntry` en 4.x, `ItemNotFound` dans la
documentation de `keyring-core`).

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

```bash
cd src-tauri && cargo test trousseau 2>&1 | tail -8
```

Attendu : `test result: ok. 3 passed; 1 ignored`.

- [ ] **Step 5: Vérifier l'aller-retour réel**

```bash
cd src-tauri && cargo test trousseau -- --ignored --nocapture 2>&1 | tail -8
```

Attendu : `test result: ok. 1 passed`. macOS peut demander l'autorisation
d'accès au trousseau — l'accorder. Contrôler ensuite que le token de dev est
intact :

```bash
security find-generic-password -s raindrop-api-token -a "$USER" -w | head -c 8; echo "…"
```

Attendu : les huit premiers caractères du vrai token, **pas**
`jeton-de-test`.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/trousseau.rs src-tauri/src/lib.rs src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -F - <<'EOF'
feat(tauri): token Raindrop au trousseau macOS

Plan 3/3, Task 5. Service « raindrop-api-token », compte $USER : les
MÊMES que scripts/dev-sidecar.sh, pour que le développement et
l'application partagent un seul enregistrement — sinon le token se
saisirait deux fois et une rotation n'en changerait qu'un.

Précision de vocabulaire sur la spec §6 : elle dit « plugin Tauri
(Keychain) », qui n'existe pas. La voie réelle est le crate keyring,
dont la feature par défaut v1 tire apple-native-keyring-store/keychain.
Le secret va bien au trousseau macOS.

L'absence de token rend Ok(None), jamais une erreur : c'est l'état normal
du premier lancement (spec §6), pas une panne.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 6: Lancer, attendre et arrêter le sidecar

**Files:**
- Create: `src-tauri/src/sidecar.rs`
- Modify: `src-tauri/src/lib.rs` (déclarer `mod sidecar;`)

**Interfaces:**
- Consumes: Task 2 (`jeton`), Task 3 (`node::Verdict`), Task 4 (`verrou`),
  Task 5 (`trousseau`).
- Produces:
  - `sidecar::Reglages { node, base, token_raindrop, token_local, dossier_donnees }`.
  - `sidecar::Sidecar` avec `pid() -> i32` et `arreter(Duration)`.
  - `sidecar::lancer(&Reglages) -> std::io::Result<Sidecar>`.
  - `sidecar::attendre_port(&Path, Duration) -> Option<u16>`.
  - `sidecar::vivant(i32) -> bool`, `sidecar::terminer(i32, Duration)`.
  - `sidecar::entree(&Path) -> PathBuf`, `sidecar::entree_mcp(&Path) -> PathBuf`.
  Consommé par Task 7.

**Chemins, vérifiés le 2026-09-17 :** `npm run build:sidecar` (`tsc`,
`outDir: dist-sidecar`, `rootDir: .`) produit
**`dist-sidecar/sidecar/index.js`**. Le serveur MCP épinglé est en
`node_modules/@kud/mcp-raindrop-io/dist/index.js`. Ces deux chemins sont
relatifs à une **base** : la racine du dépôt en développement, le dossier de
ressources du `.app` une fois empaqueté (Task 12).

- [ ] **Step 1: Écrire les tests qui échouent**

Seuls les chemins sont purs ; le reste est vérifié à la main au Step 5
(spec §8 : pas d'E2E Tauri en Phase 1).

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    #[test]
    fn l_entree_du_sidecar_est_celle_que_tsc_produit() {
        // Vérifié le 2026-09-17 : tsconfig.json a outDir "dist-sidecar" et
        // rootDir "." — l'entrée sort donc à dist-sidecar/sidecar/index.js,
        // et non dist-sidecar/index.js.
        assert_eq!(
            entree(&PathBuf::from("/base")),
            PathBuf::from("/base/dist-sidecar/sidecar/index.js")
        );
    }

    #[test]
    fn l_entree_mcp_pointe_le_paquet_epingle() {
        // CLAUDE.md : spawn direct du JS, jamais `npx @latest`.
        assert_eq!(
            entree_mcp(&PathBuf::from("/base")),
            PathBuf::from("/base/node_modules/@kud/mcp-raindrop-io/dist/index.js")
        );
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
cd src-tauri && cargo test sidecar 2>&1 | tail -12
```

Attendu : ÉCHEC de compilation — `cannot find function 'entree'`,
`cannot find function 'entree_mcp'`.

- [ ] **Step 3: Écrire l'implémentation**

En tête de `src-tauri/src/sidecar.rs` :

```rust
//! Cycle de vie du sidecar Node, vu depuis Tauri (spec §3.6).

use std::fs::File;
use std::path::{Path, PathBuf};
use std::process::{Child, Command, Stdio};
use std::time::{Duration, Instant};

use crate::verrou;

/// `tsc` (outDir `dist-sidecar`, rootDir `.`) produit cette arborescence —
/// vérifié le 2026-09-17. Pas `dist-sidecar/index.js`.
pub fn entree(base: &Path) -> PathBuf {
    base.join("dist-sidecar/sidecar/index.js")
}

/// Le serveur MCP épinglé à 1.3.1 (CLAUDE.md) : spawn direct du JS.
pub fn entree_mcp(base: &Path) -> PathBuf {
    base.join("node_modules/@kud/mcp-raindrop-io/dist/index.js")
}

pub struct Reglages {
    /// Chemin absolu de l'interpréteur, résolu par `node::resoudre`.
    pub node: PathBuf,
    /// Racine du dépôt en dev, dossier de ressources du .app en production.
    pub base: PathBuf,
    pub token_raindrop: String,
    pub token_local: String,
    pub dossier_donnees: PathBuf,
}

pub struct Sidecar {
    enfant: Child,
}

impl Sidecar {
    pub fn pid(&self) -> i32 {
        self.enfant.id() as i32
    }

    /// SIGTERM d'abord, et non `Child::kill()` (qui envoie SIGKILL) : le
    /// sidecar a un gestionnaire d'arrêt qui vide sa mémoire des origines et
    /// efface le lockfile. Le tuer net perdrait les deux.
    pub fn arreter(&mut self, grace: Duration) {
        terminer(self.pid(), grace);
        let _ = self.enfant.wait();
    }
}

/// Lance le sidecar. Les deux tokens passent par l'environnement : le token
/// Raindrop ne traverse jamais HTTP (spec §6), le token local n'est jamais
/// écrit sur disque (spec §3.7).
///
/// `RAINDROP_MCP_ENTRY` est transmis EXPLICITEMENT : son défaut, dans
/// `sidecar/config.ts`, est relatif au module du sidecar et ne survit pas à
/// l'empaquetage.
pub fn lancer(r: &Reglages) -> std::io::Result<Sidecar> {
    // stdout/stderr vers un fichier : le sidecar journalise en JSONL de son
    // côté, mais un échec AVANT que son journal n'existe (Node qui refuse le
    // fichier, import cassé) ne se lirait nulle part sans cela.
    std::fs::create_dir_all(r.dossier_donnees.join("logs"))?;
    let journal = r.dossier_donnees.join("logs/sidecar-stdio.log");
    let sortie = File::create(&journal)?;
    let erreurs = sortie.try_clone()?;

    let enfant = Command::new(&r.node)
        .arg(entree(&r.base))
        .env("MCP_RAINDROPIO_TOKEN", &r.token_raindrop)
        .env("LOCAL_API_TOKEN", &r.token_local)
        .env("RAINDROP_MCP_ENTRY", entree_mcp(&r.base))
        .env("APPDATA_DIR", &r.dossier_donnees)
        .stdin(Stdio::null())
        .stdout(Stdio::from(sortie))
        .stderr(Stdio::from(erreurs))
        .spawn()?;
    Ok(Sidecar { enfant })
}

/// Attend que le lockfile porte un port > 0 (spec §3.6 : `port: 0` = binding
/// en cours).
///
/// ATTENTION : l'appelant DOIT avoir effacé un lockfile précédent, sinon le
/// port d'un sidecar mort est lu au premier tour et rendu comme s'il était
/// neuf.
pub fn attendre_port(dossier: &Path, delai: Duration) -> Option<u16> {
    let debut = Instant::now();
    loop {
        if let Ok(texte) = std::fs::read_to_string(verrou::chemin(dossier)) {
            if let Some(port) = verrou::lire(&texte).as_ref().and_then(verrou::port_pret) {
                return Some(port);
            }
        }
        if debut.elapsed() >= delai {
            return None;
        }
        std::thread::sleep(Duration::from_millis(100));
    }
}

/// `kill -0` : le processus existe-t-il ? On passe par /bin/kill plutôt que
/// d'ajouter `libc` pour trois appels par lancement.
pub fn vivant(pid: i32) -> bool {
    Command::new("/bin/kill")
        .arg("-0")
        .arg(pid.to_string())
        .stdout(Stdio::null())
        .stderr(Stdio::null())
        .status()
        .map(|s| s.success())
        .unwrap_or(false)
}

/// SIGTERM, puis SIGKILL si le processus tient encore après `grace`.
pub fn terminer(pid: i32, grace: Duration) {
    let signal = |s: &str| {
        let _ = Command::new("/bin/kill")
            .arg(s)
            .arg(pid.to_string())
            .stdout(Stdio::null())
            .stderr(Stdio::null())
            .status();
    };
    signal("-TERM");
    let debut = Instant::now();
    while debut.elapsed() < grace {
        if !vivant(pid) {
            return;
        }
        std::thread::sleep(Duration::from_millis(100));
    }
    signal("-KILL");
}
```

Et dans `src-tauri/src/lib.rs` :

```rust
mod sidecar;
```

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

```bash
cd src-tauri && cargo test sidecar 2>&1 | tail -8
```

Attendu : `test result: ok. 2 passed`.

- [ ] **Step 5: Vérification manuelle du lancement réel**

Encore hors de Tauri : un petit programme d'essai prouve que le sidecar
démarre et publie son port, avec les vrais chemins.

```bash
cd /Users/davidbaudoin/Documents/GitHub/Raindrop-GUI
npm run build:sidecar
pkill -f "dist-sidecar/sidecar/index.js" 2>/dev/null
rm -f ~/Library/Application\ Support/Raindrop-GUI/sidecar.json
TOKEN=$(security find-generic-password -s raindrop-api-token -a "$USER" -w)
MCP_RAINDROPIO_TOKEN="$TOKEN" \
LOCAL_API_TOKEN="essai-local" \
RAINDROP_MCP_ENTRY="$PWD/node_modules/@kud/mcp-raindrop-io/dist/index.js" \
  /opt/homebrew/bin/node dist-sidecar/sidecar/index.js &
sleep 4
PORT=$(python3 -c "import json,os;print(json.load(open(os.path.expanduser('~/Library/Application Support/Raindrop-GUI/sidecar.json')))['port'])")
echo "port=$PORT"
curl -s -H "Authorization: Bearer essai-local" "http://127.0.0.1:$PORT/api/health"
echo
pkill -f "dist-sidecar/sidecar/index.js"
```

Attendu : `port=` suivi d'un port > 0, puis
`{"status":"ok","mcp":"connected"}`. Si `mcp` vaut `starting`, attendre et
refaire l'appel. **Couper le sidecar à la fin** — le `pkill` final s'en
charge.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/sidecar.rs src-tauri/src/lib.rs
git commit -F - <<'EOF'
feat(tauri): lancer, attendre et arrêter le sidecar

Plan 3/3, Task 6. Les deux tokens passent par l'environnement : le token
Raindrop ne traverse jamais HTTP (spec §6), le token local n'est jamais
écrit sur disque (spec §3.7).

Trois détails qui auraient coûté cher plus tard :
- RAINDROP_MCP_ENTRY est transmis EXPLICITEMENT — son défaut est relatif
  au module du sidecar et ne survit pas à l'empaquetage ;
- l'arrêt passe par SIGTERM, pas Child::kill() qui envoie SIGKILL : le
  sidecar a un gestionnaire d'arrêt qui vide sa mémoire des origines et
  efface son lockfile ;
- attendre_port() exige que l'appelant ait effacé le lockfile précédent,
  sinon le port d'un sidecar mort est lu au premier tour.

Les chemins sont testés, le reste vérifié à la main (spec §8 : pas d'E2E
Tauri en Phase 1).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 7: L'état de connexion et les deux commandes

**Files:**
- Create: `src-tauri/src/commandes.rs`
- Modify: `src-tauri/src/lib.rs` (assemblage complet)

**Interfaces:**
- Consumes: Tasks 2 à 6.
- Produces, côté webview (contrat consommé par Task 8) :
  - commande `etat_connexion() -> EtatConnexion` (rend l'état mémorisé)
  - commande `relancer() -> EtatConnexion` (**rejoue** la séquence)
  - commande `enregistrer_jeton(jetonRaindrop: string) -> EtatConnexion`
  - `EtatConnexion` sérialisé avec un discriminant `kind` :
    - `{ "kind": "pret", "port": 51234, "token": "…" }`
    - `{ "kind": "jeton_requis" }`
    - `{ "kind": "node_absent", "detail": "…" }`
    - `{ "kind": "panne", "detail": "…" }`

**Séquence de démarrage.** Elle tourne dans un fil de fond pour que la fenêtre
paraisse tout de suite ; `etat_connexion` attend son résultat (l'appel est
`await` côté JS, rien ne gèle). Dans l'ordre : résoudre `node` → lire le
trousseau → terminer un sidecar survivant (D2) → **effacer le lockfile** →
lancer → attendre le port.

L'effacement du lockfile n'est pas une précaution de style : sans lui,
`attendre_port` lit au premier tour le port du sidecar qu'on vient de tuer et
le rend comme s'il était neuf.

- [ ] **Step 1: Écrire les tests qui échouent**

Ce qui est testable ici sans fenêtre, c'est la **forme du contrat** — le
webview la lit littéralement, et une faute de sérialisation ne se verrait
qu'à l'écran.

Dans `src-tauri/src/commandes.rs` :

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn json(e: &EtatConnexion) -> String {
        serde_json::to_string(e).unwrap()
    }

    #[test]
    fn pret_porte_son_port_et_son_token() {
        let e = EtatConnexion::Pret { port: 51234, token: "abc".into() };
        assert_eq!(json(&e), r#"{"kind":"pret","port":51234,"token":"abc"}"#);
    }

    #[test]
    fn les_etats_sans_donnee_ne_portent_que_leur_kind() {
        // Le front distingue les écrans sur `kind` seul : une faute ici est
        // invisible côté Rust et rend un écran blanc côté webview.
        assert_eq!(json(&EtatConnexion::JetonRequis), r#"{"kind":"jeton_requis"}"#);
    }

    #[test]
    fn node_absent_et_panne_portent_leur_detail() {
        let n = EtatConnexion::NodeAbsent { detail: "rien trouvé".into() };
        assert_eq!(json(&n), r#"{"kind":"node_absent","detail":"rien trouvé"}"#);
        let p = EtatConnexion::Panne { detail: "boum".into() };
        assert_eq!(json(&p), r#"{"kind":"panne","detail":"boum"}"#);
    }

    #[test]
    fn un_node_trop_vieux_dit_sa_version_et_le_plancher() {
        // Spec §3.2 et §7 : l'écran doit pouvoir écrire une instruction
        // utile, donc porter le constat, pas un « erreur ».
        let e = verdict_en_etat(crate::node::Verdict::TropVieux {
            chemin: "/usr/bin/node".into(),
            version: "v18.19.0".into(),
        });
        let EtatConnexion::NodeAbsent { detail } = e else { panic!("attendu node_absent") };
        assert!(detail.contains("v18.19.0"), "la version trouvée : {detail}");
        assert!(detail.contains("20"), "le plancher exigé : {detail}");
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
cd src-tauri && cargo test commandes 2>&1 | tail -12
```

Attendu : ÉCHEC de compilation — `cannot find type 'EtatConnexion'`,
`cannot find function 'verdict_en_etat'`.

- [ ] **Step 3: Écrire `src-tauri/src/commandes.rs`**

```rust
//! L'état de la connexion locale, et les deux commandes du webview.

use std::path::PathBuf;
use std::sync::{Condvar, Mutex};
use std::time::Duration;

use serde::Serialize;
use tauri::State;

use crate::{node, sidecar, trousseau, verrou};

/// Le sidecar a jusque-là pour publier son port. Large : au premier
/// lancement, Node compile et le serveur MCP se connecte.
const DELAI_PORT: Duration = Duration::from_secs(25);
/// Puis `etat_connexion` cesse d'attendre la séquence elle-même.
const DELAI_SEQUENCE: Duration = Duration::from_secs(40);
/// Laissé à un sidecar pour s'arrêter proprement avant SIGKILL.
const GRACE: Duration = Duration::from_secs(3);

#[derive(Debug, Clone, Serialize)]
#[serde(tag = "kind", rename_all = "snake_case")]
pub enum EtatConnexion {
    Pret { port: u16, token: String },
    JetonRequis,
    NodeAbsent { detail: String },
    Panne { detail: String },
}

pub struct Etat {
    pub token_local: String,
    pub base: PathBuf,
    pub dossier: PathBuf,
    sidecar: Mutex<Option<sidecar::Sidecar>>,
    resolu: (Mutex<Option<EtatConnexion>>, Condvar),
}

impl Etat {
    pub fn new(token_local: String, base: PathBuf, dossier: PathBuf) -> Self {
        Self {
            token_local,
            base,
            dossier,
            sidecar: Mutex::new(None),
            resolu: (Mutex::new(None), Condvar::new()),
        }
    }

    pub fn poser(&self, e: EtatConnexion) {
        *self.resolu.0.lock().unwrap() = Some(e);
        self.resolu.1.notify_all();
    }

    /// Attend que la séquence de démarrage ait tranché.
    pub fn attendre(&self) -> EtatConnexion {
        let (m, cv) = &self.resolu;
        let mut g = m.lock().unwrap();
        while g.is_none() {
            let (suite, delai) = cv.wait_timeout(g, DELAI_SEQUENCE).unwrap();
            g = suite;
            if delai.timed_out() && g.is_none() {
                return EtatConnexion::Panne {
                    detail: "le sidecar local n'a pas démarré dans le temps imparti".into(),
                };
            }
        }
        g.clone().expect("posé")
    }

    /// Arrêt propre à la fermeture de l'application.
    pub fn arreter_sidecar(&self) {
        if let Some(mut s) = self.sidecar.lock().unwrap().take() {
            s.arreter(GRACE);
        }
    }
}

/// Traduit un verdict Node en état montrable. Pur — testé.
pub fn verdict_en_etat(v: node::Verdict) -> EtatConnexion {
    match v {
        node::Verdict::Trouve { .. } => EtatConnexion::Panne {
            detail: "verdict_en_etat appelé sur un node conforme".into(),
        },
        node::Verdict::TropVieux { chemin, version } => EtatConnexion::NodeAbsent {
            detail: format!(
                "Node {version} trouvé ({}), mais la version {} ou supérieure est requise.",
                chemin.display(),
                node::MAJEURE_MINIMALE
            ),
        },
        node::Verdict::Absent => EtatConnexion::NodeAbsent {
            detail: format!(
                "Aucun interpréteur Node n'a été trouvé. Node {} ou supérieur est requis.",
                node::MAJEURE_MINIMALE
            ),
        },
    }
}

/// La séquence complète. Rendue dans un fil de fond au démarrage.
pub fn sequence(etat: &Etat) -> EtatConnexion {
    let chemin_node = match node::resoudre() {
        node::Verdict::Trouve { chemin, .. } => chemin,
        autre => return verdict_en_etat(autre),
    };
    let token_raindrop = match trousseau::lire() {
        Ok(Some(t)) => t,
        Ok(None) => return EtatConnexion::JetonRequis, // premier lancement (§6)
        Err(detail) => return EtatConnexion::Panne { detail },
    };
    lancer_sidecar(etat, chemin_node, &token_raindrop)
}

fn lancer_sidecar(etat: &Etat, chemin_node: PathBuf, token_raindrop: &str) -> EtatConnexion {
    let fichier = verrou::chemin(&etat.dossier);

    // Décision D2 : un sidecar survivant écoute avec l'ANCIEN token local.
    let precedent = std::fs::read_to_string(&fichier)
        .ok()
        .and_then(|t| verrou::lire(&t));
    if let verrou::Decision::Terminer(pid) = verrou::decider(precedent.as_ref(), sidecar::vivant) {
        sidecar::terminer(pid, GRACE);
    }
    // INDISPENSABLE : sans cet effacement, attendre_port lit au premier tour
    // le port du sidecar qu'on vient de tuer et le rend comme s'il était neuf.
    let _ = std::fs::remove_file(&fichier);

    let reglages = sidecar::Reglages {
        node: chemin_node,
        base: etat.base.clone(),
        token_raindrop: token_raindrop.to_string(),
        token_local: etat.token_local.clone(),
        dossier_donnees: etat.dossier.clone(),
    };
    let enfant = match sidecar::lancer(&reglages) {
        Ok(s) => s,
        Err(e) => {
            return EtatConnexion::Panne {
                detail: format!("lancement du sidecar impossible : {e}"),
            }
        }
    };
    let pid = enfant.pid();
    *etat.sidecar.lock().unwrap() = Some(enfant);

    match sidecar::attendre_port(&etat.dossier, DELAI_PORT) {
        Some(port) => EtatConnexion::Pret {
            port,
            token: etat.token_local.clone(),
        },
        None => {
            sidecar::terminer(pid, GRACE);
            EtatConnexion::Panne {
                detail: format!(
                    "le sidecar n'a pas publié de port. Journal : {}",
                    etat.dossier.join("logs/sidecar-stdio.log").display()
                ),
            }
        }
    }
}

#[tauri::command]
pub fn etat_connexion(etat: State<'_, Etat>) -> EtatConnexion {
    etat.attendre()
}

/// Rejoue la séquence entière.
///
/// INDISPENSABLE au bouton « Réessayer » des écrans d'amorçage : `attendre`
/// rend l'état DÉJÀ mémorisé, si bien qu'un réessai qui repasserait par
/// `etat_connexion` rendrait éternellement la même panne. C'est ce qui
/// transformerait l'écran de panne en cul-de-sac : une panne passagère, ou un
/// jeton refusé, enfermeraient l'utilisateur pour de bon.
#[tauri::command]
pub fn relancer(etat: State<'_, Etat>) -> EtatConnexion {
    let e = sequence(&etat);
    etat.poser(e.clone());
    e
}

/// Premier lancement (§6) : le token saisi est enregistré au trousseau, puis
/// le sidecar démarre. Sert aussi à REMPLACER un token refusé — la même
/// commande, le même chemin.
#[tauri::command]
pub fn enregistrer_jeton(jeton_raindrop: String, etat: State<'_, Etat>) -> EtatConnexion {
    let jeton = jeton_raindrop.trim().to_string();
    if jeton.is_empty() {
        return EtatConnexion::Panne { detail: "token vide".into() };
    }
    if let Err(detail) = trousseau::ecrire(&jeton) {
        return EtatConnexion::Panne { detail };
    }
    let chemin_node = match node::resoudre() {
        node::Verdict::Trouve { chemin, .. } => chemin,
        autre => return verdict_en_etat(autre),
    };
    let e = lancer_sidecar(&etat, chemin_node, &jeton);
    etat.poser(e.clone());
    e
}
```

- [ ] **Step 4: Écrire l'assemblage dans `src-tauri/src/lib.rs`**

**Conserver le bloc `#[cfg(test)] mod tests` de la Task 1 en fin de fichier** :
cette réécriture le supprimerait sinon, et le compte de tests du Step 5 le
comprend.

```rust
mod commandes;
mod jeton;
mod node;
mod sidecar;
mod trousseau;
mod verrou;

use std::path::PathBuf;
use tauri::Manager;

use commandes::Etat;

/// Racine des ressources : le dépôt en développement, le dossier embarqué
/// dans le .app une fois empaqueté (Task 12).
fn base(app: &tauri::AppHandle) -> PathBuf {
    if cfg!(debug_assertions) {
        PathBuf::from(env!("CARGO_MANIFEST_DIR"))
            .parent()
            .expect("src-tauri a un parent")
            .to_path_buf()
    } else {
        app.path()
            .resource_dir()
            .expect("dossier de ressources")
            .join("ressources")
    }
}

pub fn run() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![
            commandes::etat_connexion,
            commandes::enregistrer_jeton,
            commandes::relancer
        ])
        .setup(|app| {
            let token_local = match jeton::engendrer() {
                Ok(t) => t,
                // Sans aléa système, rien de sûr n'est possible : mieux vaut
                // s'arrêter net que servir une superficie HTTP devinable.
                Err(e) => return Err(format!("aléa système illisible : {e}").into()),
            };
            let etat = Etat::new(token_local, base(app.handle()), verrou::dossier_donnees());
            app.manage(etat);

            // La séquence tourne à côté : la fenêtre paraît tout de suite, et
            // `etat_connexion` attend le verdict (appel `await` côté JS).
            let handle = app.handle().clone();
            std::thread::spawn(move || {
                let etat = handle.state::<Etat>();
                let resultat = commandes::sequence(&etat);
                etat.poser(resultat);
            });
            Ok(())
        })
        .build(tauri::generate_context!())
        .expect("erreur au lancement de l'application Tauri")
        .run(|app, evenement| {
            // Le sidecar est notre enfant : le laisser derrière laisserait un
            // port ouvert et un lockfile menteur.
            if let tauri::RunEvent::Exit = evenement {
                if let Some(etat) = app.try_state::<Etat>() {
                    etat.arreter_sidecar();
                }
            }
        });
}
```

- [ ] **Step 5: Lancer les tests et vérifier qu'ils passent**

```bash
cd src-tauri && cargo test 2>&1 | tail -10
```

Attendu : `test result: ok. 24 passed; 2 ignored` — 1 harnais + 3 jeton +
6 node + 6 verrou + 4 trousseau + 2 sidecar + 4 commandes = 26 déclarés, dont
`ou_est_node_sur_cette_machine` et `aller_retour_ecrire_lire_effacer` marqués
`#[ignore]`. Le compte exact importe moins que **zéro échec**.

- [ ] **Step 6: Vérification manuelle — le sidecar démarre sous Tauri**

Le front n'appelle pas encore les commandes (Task 8) ; on les sollicite à la
main depuis la console du webview.

```bash
pkill -f "dist-sidecar/sidecar/index.js" 2>/dev/null
npm run build:sidecar
npm run tauri:dev
```

Dans la fenêtre, ouvrir l'inspecteur (clic droit → Inspect Element) et taper :

```js
await window.__TAURI__.core.invoke("etat_connexion")
```

**Si rien ne revient et que l'application semble figée :** chercher une
demande d'autorisation du trousseau, que macOS peut avoir ouverte **derrière**
la fenêtre. La signature étant ad-hoc et régénérée à chaque construction,
« Toujours autoriser » ne tient pas d'un build au suivant.

Attendu : `{kind: "pret", port: <nombre > 0>, token: "<64 caractères hex>"}`.

**Observer au passage — la fenêtre peint-elle pendant l'attente ?**
`etat_connexion` bloque jusqu'à 40 s et `enregistrer_jeton` jusqu'à 25 s, et
ce sont des commandes **synchrones**. Pendant que le sidecar démarre, la
fenêtre doit afficher l'interface React, pas rester blanche ni tourner la
roue. **Si elle gèle**, le correctif est local et ne change aucune décision :
passer les deux commandes en `async fn` et déplacer leur corps bloquant dans
`tauri::async_runtime::spawn_blocking`.

Puis vérifier que le sidecar tourne vraiment :

```bash
pgrep -fl "dist-sidecar/sidecar/index.js"
```

Attendu : une ligne. **Quitter l'application (⌘Q), puis refaire `pgrep`** :
attendu, aucune ligne — l'arrêt propre marche.

- [ ] **Step 7: Vérifier la reprise après un sidecar survivant (décision D2)**

```bash
# lancer un sidecar « orphelin » avec un AUTRE token local
TOKEN=$(security find-generic-password -s raindrop-api-token -a "$USER" -w)
MCP_RAINDROPIO_TOKEN="$TOKEN" LOCAL_API_TOKEN="ancien-token" \
RAINDROP_MCP_ENTRY="$PWD/node_modules/@kud/mcp-raindrop-io/dist/index.js" \
  /opt/homebrew/bin/node dist-sidecar/sidecar/index.js &
sleep 4
ORPHELIN=$(pgrep -f "dist-sidecar/sidecar/index.js")
echo "orphelin=$ORPHELIN"
npm run tauri:dev
```

Dans la console du webview : `await window.__TAURI__.core.invoke("etat_connexion")`.
Attendu : `kind: "pret"` avec un port, et dans un autre terminal
`pgrep -f dist-sidecar` doit montrer un PID **différent** de `$ORPHELIN` —
l'orphelin a été terminé, pas réutilisé. Quitter, puis `pkill -f dist-sidecar`
par sécurité.

- [ ] **Step 8: Commit**

```bash
git add src-tauri/src/commandes.rs src-tauri/src/lib.rs
git commit -F - <<'EOF'
feat(tauri): l'état de connexion et les deux commandes du webview

Plan 3/3, Task 7. La séquence de démarrage — résoudre node, lire le
trousseau, terminer un sidecar survivant, effacer le lockfile, lancer,
attendre le port — tourne dans un fil de fond : la fenêtre paraît tout de
suite et `etat_connexion` attend le verdict, l'appel étant `await` côté JS.

L'effacement du lockfile avant le lancement n'est pas décoratif : sans
lui, attendre_port lit au premier tour le port du sidecar qu'on vient de
tuer et le rend comme s'il était neuf.

Quatre états, et un seul discriminant `kind` que le front lit
littéralement : pret / jeton_requis / node_absent / panne. Leur
sérialisation est testée, car une faute y serait invisible côté Rust et
rendrait un écran blanc côté webview.

`enregistrer_jeton` sert au premier lancement ET au remplacement d'un
token refusé : même commande, même chemin.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 8: L'amorçage du front

**Files:**
- Create: `src/lib/amorce.ts`
- Create: `src/lib/amorce.test.ts`
- Modify: `src/main.tsx`
- Modify: `src/env.d.ts` (si `isTauri` exige une déclaration)
- Modify: `package.json` (`@tauri-apps/api`)

**Interfaces:**
- Consumes: Task 7 (commandes `etat_connexion`, `enregistrer_jeton`, forme de
  `EtatConnexion`).
- Produces:
  - `type EtatConnexion` (miroir TypeScript du contrat Rust).
  - `type Amorce = { ecran: "app" } | { ecran: "premier-lancement" } |
    { ecran: "diagnostic"; detail: string } | { ecran: "panne"; detail: string }`.
  - `appliquer(etat: EtatConnexion): Amorce` — **pose
    `window.RAINDROP_GUI`** quand l'état est `pret`.
  - `amorcer(): Promise<Amorce>` — lit l'état mémorisé.
  - `relancer(): Promise<Amorce>` — **rejoue** la séquence côté Rust ; c'est
    ce que doit appeler un bouton « Réessayer », `amorcer()` rendant
    éternellement le même verdict.
  Consommé par Tasks 9, 10 et `main.tsx`.

**Rappel de la décision D1 :** `connection.ts` n'est pas touché. C'est
`appliquer` qui remplit `window.RAINDROP_GUI`, si bien que `getConnection()`
reste synchrone et que `api.ts`, `sse.ts` et les trois tests de
`connection.test.ts` sont inchangés.

`isTauri()` de `@tauri-apps/api/core` teste `globalThis.isTauri` (vérifié dans
le paquet 2.11.1) : hors webview il rend `false`, et le front garde son
comportement de développement (proxy Vite, `VITE_LOCAL_API_TOKEN`).

- [ ] **Step 1: Installer l'API JS de Tauri**

```bash
npm i @tauri-apps/api@^2
```

- [ ] **Step 2: Écrire les tests qui échouent**

`src/lib/amorce.test.ts` :

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { appliquer, amorcer, relancer } from "./amorce";

const { invokeMock, isTauriMock } = vi.hoisted(() => ({
  invokeMock: vi.fn(),
  isTauriMock: vi.fn(),
}));
vi.mock("@tauri-apps/api/core", () => ({ invoke: invokeMock, isTauri: isTauriMock }));

beforeEach(() => {
  invokeMock.mockReset();
  isTauriMock.mockReset().mockReturnValue(true);
});
afterEach(() => {
  delete window.RAINDROP_GUI;
});

describe("appliquer", () => {
  // Le cœur de la décision D1 : c'est ICI que le global se remplit, ce qui
  // laisse getConnection() synchrone et api.ts/sse.ts intouchés.
  it("« pret » pose window.RAINDROP_GUI et mène à l'application", () => {
    const a = appliquer({ kind: "pret", port: 51234, token: "abc" });
    expect(a).toEqual({ ecran: "app" });
    expect(window.RAINDROP_GUI).toEqual({ port: 51234, token: "abc" });
  });

  it("« jeton_requis » mène au premier lancement, sans rien poser", () => {
    expect(appliquer({ kind: "jeton_requis" })).toEqual({ ecran: "premier-lancement" });
    expect(window.RAINDROP_GUI).toBeUndefined();
  });

  it("« node_absent » et « panne » portent leur détail à l'écran", () => {
    expect(appliquer({ kind: "node_absent", detail: "rien trouvé" })).toEqual({
      ecran: "diagnostic",
      detail: "rien trouvé",
    });
    expect(appliquer({ kind: "panne", detail: "boum" })).toEqual({
      ecran: "panne",
      detail: "boum",
    });
  });
});

describe("amorcer", () => {
  it("hors Tauri, rend l'application sans appeler quoi que ce soit", async () => {
    // Développement au navigateur : le proxy Vite et VITE_LOCAL_API_TOKEN
    // font le travail, il n'y a aucune commande à invoquer.
    isTauriMock.mockReturnValue(false);
    expect(await amorcer()).toEqual({ ecran: "app" });
    expect(invokeMock).not.toHaveBeenCalled();
  });

  it("sous Tauri, demande l'état et l'applique", async () => {
    invokeMock.mockResolvedValue({ kind: "pret", port: 7, token: "t" });
    expect(await amorcer()).toEqual({ ecran: "app" });
    expect(invokeMock).toHaveBeenCalledWith("etat_connexion");
    expect(window.RAINDROP_GUI).toEqual({ port: 7, token: "t" });
  });

  // Sans cette garde, une commande qui rejette laisse une page blanche : le
  // rendu n'a jamais lieu et rien n'explique pourquoi.
  it("une commande qui rejette devient un écran de panne, pas une page blanche", async () => {
    invokeMock.mockRejectedValue(new Error("pont IPC coupé"));
    expect(await amorcer()).toEqual({ ecran: "panne", detail: "pont IPC coupé" });
  });
});

describe("relancer", () => {
  // Le contrat qui évite le cul-de-sac : « Réessayer » doit REJOUER la
  // séquence côté Rust. Passer par etat_connexion rendrait l'état mémorisé,
  // donc la même panne, indéfiniment.
  it("appelle la commande qui rejoue, pas celle qui relit", async () => {
    invokeMock.mockResolvedValue({ kind: "pret", port: 9, token: "t" });
    expect(await relancer()).toEqual({ ecran: "app" });
    expect(invokeMock).toHaveBeenCalledWith("relancer");
  });
});
```

- [ ] **Step 3: Lancer les tests et vérifier qu'ils échouent**

```bash
npx vitest run src/lib/amorce.test.ts 2>&1 | tail -10
```

Attendu : ÉCHEC — `Failed to resolve import "./amorce"`.

- [ ] **Step 4: Écrire `src/lib/amorce.ts`**

```ts
import { invoke, isTauri } from "@tauri-apps/api/core";

// Miroir du contrat Rust (src-tauri/src/commandes.rs, EtatConnexion).
// Le discriminant `kind` est sérialisé en snake_case côté Rust ; ces noms
// doivent correspondre exactement.
export type EtatConnexion =
  | { kind: "pret"; port: number; token: string }
  | { kind: "jeton_requis" }
  | { kind: "node_absent"; detail: string }
  | { kind: "panne"; detail: string };

export type Amorce =
  | { ecran: "app" }
  | { ecran: "premier-lancement" }
  | { ecran: "diagnostic"; detail: string }
  | { ecran: "panne"; detail: string };

/**
 * Traduit l'état rendu par Tauri en écran à montrer — et, quand tout est
 * prêt, POSE `window.RAINDROP_GUI`.
 *
 * C'est la charnière de la décision D1 : `connection.ts` lit ce global
 * synchroniquement, à chaque requête. En le remplissant ici, avant le
 * premier rendu, on garde `getConnection()` synchrone et on ne touche ni
 * `api.ts`, ni `sse.ts`, ni leurs tests.
 */
export function appliquer(etat: EtatConnexion): Amorce {
  switch (etat.kind) {
    case "pret":
      window.RAINDROP_GUI = { port: etat.port, token: etat.token };
      return { ecran: "app" };
    case "jeton_requis":
      return { ecran: "premier-lancement" };
    case "node_absent":
      return { ecran: "diagnostic", detail: etat.detail };
    case "panne":
      return { ecran: "panne", detail: etat.detail };
  }
}

async function demander(commande: "etat_connexion" | "relancer"): Promise<Amorce> {
  // Hors webview (développement au navigateur), le proxy Vite et
  // VITE_LOCAL_API_TOKEN suffisent : rien à demander à personne.
  if (!isTauri()) return { ecran: "app" };
  try {
    return appliquer(await invoke<EtatConnexion>(commande));
  } catch (e) {
    // Sans cette garde, un rejet laisse une page blanche : le rendu n'a
    // jamais lieu et rien n'explique pourquoi.
    return { ecran: "panne", detail: e instanceof Error ? e.message : String(e) };
  }
}

/** Au premier rendu : lit l'état auquel la séquence de démarrage a abouti. */
export const amorcer = (): Promise<Amorce> => demander("etat_connexion");

/**
 * Ce qu'appelle « Réessayer ». `etat_connexion` rend l'état MÉMORISÉ : un
 * réessai qui passerait par elle rendrait éternellement la même panne, et
 * l'écran de panne serait un cul-de-sac.
 */
export const relancer = (): Promise<Amorce> => demander("relancer");
```

- [ ] **Step 5: Lancer les tests et vérifier qu'ils passent**

```bash
npx vitest run src/lib/amorce.test.ts 2>&1 | tail -6
```

Attendu : `Tests  7 passed (7)`.

- [ ] **Step 6: Câbler `src/main.tsx`**

Remplacer l'appel à `createRoot(...).render(...)` par un rendu différé, après
l'amorçage. Les écrans de Tasks 9 et 10 n'existent pas encore : les brancher
ici provoquerait une erreur de compilation, on met donc d'abord un rendu qui
**ne traite que `"app"`**, et les deux tasks suivantes complètent le
branchement.

```tsx
// À ajouter aux imports de main.tsx : `useState` depuis react (Racine le
// consomme dès la Task 9), et l'amorçage.
import React, { useState } from "react";
import { amorcer, type Amorce } from "./lib/amorce";

function Racine({ amorce }: { amorce: Amorce }) {
  // Les écrans de premier lancement et de diagnostic arrivent aux Tasks 9
  // et 10 ; en attendant, tout mène à l'application.
  if (amorce.ecran !== "app") {
    return <p className="p-6 text-app-ink">{amorce.ecran} — écran à venir</p>;
  }
  return <App />;
}

void amorcer().then((amorce) => {
  ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
      <QueryClientProvider client={queryClient}>
        <AppStateProvider>
          <DragProvider>
            <Racine amorce={amorce} />
          </DragProvider>
        </AppStateProvider>
      </QueryClientProvider>
    </React.StrictMode>,
  );
});
```

`then` plutôt qu'un `await` de haut niveau : cela évite d'imposer une cible de
compilation particulière au bundle.

- [ ] **Step 7: Vérifier que rien n'a bougé ailleurs**

```bash
npm run typecheck:front && npm test 2>&1 | grep -E "Tests |FAIL"
```

Attendu : aucune erreur de type, et **les trois tests de
`src/lib/connection.test.ts` passent toujours** — décision D1 tenue.

- [ ] **Step 8: Vérification manuelle — le développement au navigateur marche toujours**

```bash
./scripts/dev-sidecar.sh &
sleep 5
VITE_LOCAL_API_TOKEN=dev-local-token npm run dev
```

Ouvrir `http://localhost:5173` : la bibliothèque se charge comme avant
(`isTauri()` rend `false`, aucune commande n'est appelée). **Couper les deux
serveurs ensuite.**

- [ ] **Step 9: Commit**

```bash
git add src/lib/amorce.ts src/lib/amorce.test.ts src/main.tsx package.json package-lock.json
git commit -F - <<'EOF'
feat(front): amorçage — demander son état à Tauri avant de rendre

Plan 3/3, Task 8. Décision D1 du plan : le webview reçoit sa connexion par
une commande (asynchrone), mais connection.ts lit un global de façon
synchrone à chaque requête. La charnière est `appliquer()`, qui POSE
window.RAINDROP_GUI avant le premier rendu — si bien que getConnection()
reste synchrone et que api.ts, sse.ts et connection.test.ts sont
inchangés.

Hors webview, isTauri() rend false et rien n'est invoqué : le
développement au navigateur garde le proxy Vite et VITE_LOCAL_API_TOKEN.

Une commande qui rejette devient un écran de panne, jamais une page
blanche.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 9: L'écran de premier lancement

**Files:**
- Create: `src/components/PremierLancement.tsx`
- Create: `src/components/PremierLancement.test.tsx`
- Modify: `src/i18n/fr.ts`
- Modify: `src/main.tsx` (brancher l'écran)

**Interfaces:**
- Consumes: Task 8 (`appliquer`, `Amorce`, `EtatConnexion`), Task 7
  (commande `enregistrer_jeton`).
- Produces: `<PremierLancement onPret={(a: Amorce) => void} />`.

**Spec §6 :** « saisie du token, validation immédiate via `get_user` (affiche
le compte détecté), puis démarrage du sidecar ». L'ordre réel est contraint :
`get_user` ne peut répondre qu'à travers un sidecar déjà lancé avec ce token.
La séquence est donc **enregistrer → lancer → interroger `/api/user` →
montrer le compte → entrer**.

Le paramètre Rust `jeton_raindrop` s'invoque en `jetonRaindrop` : Tauri 2
convertit snake_case en camelCase à la frontière JS.

- [ ] **Step 1: Ajouter les textes**

Dans `src/i18n/fr.ts`, avant `"banner.unreachable"` :

```ts
  // Premier lancement (spec §6) et écrans d'amorçage.
  "boot.title": "Connecter votre compte Raindrop",
  "boot.explain": "Raindrop GUI a besoin d'un jeton d'API pour lire votre bibliothèque. Il est conservé dans le trousseau macOS et ne quitte jamais cet ordinateur.",
  "boot.where": "Raindrop.io → Settings → Integrations → Create new app → Test token",
  "boot.tokenLabel": "Jeton d'API Raindrop",
  "boot.validate": "Valider",
  "boot.checking": "Vérification…",
  "boot.account": "Compte détecté : {name} ({email}) — {count} signets",
  "boot.enter": "Ouvrir la bibliothèque",
  "boot.nodeTitle": "Node est introuvable",
  "boot.nodeHelp": "Installez Node 20 ou supérieur (par exemple « brew install node »), puis relancez l'application.",
  "boot.panneTitle": "Le service local n'a pas démarré",
  "boot.panneHelp": "Relancez l'application. Si le problème persiste, le journal de démarrage se trouve dans le dossier de données de l'application.",
```

- [ ] **Step 2: Écrire les tests qui échouent**

`src/components/PremierLancement.test.tsx` :

```tsx
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { PremierLancement } from "./PremierLancement";
import type { EtatConnexion } from "../lib/amorce";

const { invokeMock, getMock } = vi.hoisted(() => ({ invokeMock: vi.fn(), getMock: vi.fn() }));
vi.mock("@tauri-apps/api/core", () => ({ invoke: invokeMock, isTauri: () => true }));
vi.mock("../lib/api", () => ({ api: { get: getMock, send: vi.fn() } }));

const compte = { id: 1, email: "a@b.c", fullName: "Alice", pro: true, bookmarksCount: 12210 };

beforeEach(() => {
  invokeMock.mockReset().mockResolvedValue({ kind: "pret", port: 51234, token: "tok" });
  getMock.mockReset().mockResolvedValue(compte);
});
afterEach(() => {
  delete window.RAINDROP_GUI;
});

describe("PremierLancement", () => {
  it("enregistre le jeton, puis annonce le compte détecté (spec §6)", async () => {
    const user = userEvent.setup();
    render(<PremierLancement onPret={vi.fn()} />);
    await user.type(screen.getByLabelText("Jeton d'API Raindrop"), "abc123");
    await user.click(screen.getByRole("button", { name: "Valider" }));

    // Le paramètre Rust jeton_raindrop s'invoque en camelCase.
    expect(invokeMock).toHaveBeenCalledWith("enregistrer_jeton", { jetonRaindrop: "abc123" });
    await waitFor(() => expect(screen.getByText(/Alice/)).toBeInTheDocument());
    expect(screen.getByText(/12210/)).toBeInTheDocument();
  });

  it("le jeton est masqué à la saisie", () => {
    render(<PremierLancement onPret={vi.fn()} />);
    expect(screen.getByLabelText("Jeton d'API Raindrop")).toHaveAttribute("type", "password");
  });

  // Le grief à éviter : un jeton refusé laisserait un écran figé, ou pire,
  // ferait entrer dans une application qui ne peut rien lire.
  it("un jeton refusé se dit et laisse recommencer", async () => {
    const user = userEvent.setup();
    getMock.mockRejectedValue(new Error("http 401"));
    render(<PremierLancement onPret={vi.fn()} />);
    await user.type(screen.getByLabelText("Jeton d'API Raindrop"), "faux");
    await user.click(screen.getByRole("button", { name: "Valider" }));

    expect(await screen.findByRole("alert")).toHaveTextContent(/401/);
    expect(screen.getByRole("button", { name: "Valider" })).toBeEnabled();
    // Ne pas laisser une connexion à demi ouverte derrière soi.
    expect(window.RAINDROP_GUI).toBeUndefined();
  });

  it("deux clics rapides n'enregistrent qu'une fois", async () => {
    // Même garde que le Composer (lot Composer/Tags) : une double
    // validation ne doit pas lancer deux sidecars.
    const user = userEvent.setup();
    let resoudre: (v: EtatConnexion) => void = () => undefined;
    invokeMock.mockReturnValue(new Promise<EtatConnexion>((r) => { resoudre = r; }));
    render(<PremierLancement onPret={vi.fn()} />);
    await user.type(screen.getByLabelText("Jeton d'API Raindrop"), "abc");
    await user.click(screen.getByRole("button", { name: "Valider" }));
    // Le bouton porte désormais « Vérification… » : un second clic dessus
    // ne doit rien déclencher de plus.
    await user.click(screen.getByRole("button", { name: "Vérification…" }));
    expect(invokeMock).toHaveBeenCalledTimes(1);
    resoudre({ kind: "pret", port: 1, token: "t" });
  });

  it("« node_absent » ne reste pas sur cet écran : il remonte", async () => {
    const user = userEvent.setup();
    const onPret = vi.fn();
    invokeMock.mockResolvedValue({ kind: "node_absent", detail: "rien trouvé" });
    render(<PremierLancement onPret={onPret} />);
    await user.type(screen.getByLabelText("Jeton d'API Raindrop"), "abc");
    await user.click(screen.getByRole("button", { name: "Valider" }));
    await waitFor(() =>
      expect(onPret).toHaveBeenCalledWith({ ecran: "diagnostic", detail: "rien trouvé" }),
    );
  });

  it("entrer dans la bibliothèque remonte l'écran « app »", async () => {
    const user = userEvent.setup();
    const onPret = vi.fn();
    render(<PremierLancement onPret={onPret} />);
    await user.type(screen.getByLabelText("Jeton d'API Raindrop"), "abc");
    await user.click(screen.getByRole("button", { name: "Valider" }));
    await user.click(await screen.findByRole("button", { name: "Ouvrir la bibliothèque" }));
    expect(onPret).toHaveBeenCalledWith({ ecran: "app" });
  });
});
```

- [ ] **Step 3: Lancer les tests et vérifier qu'ils échouent**

```bash
npx vitest run src/components/PremierLancement.test.tsx 2>&1 | tail -8
```

Attendu : ÉCHEC — `Failed to resolve import "./PremierLancement"`.

- [ ] **Step 4: Écrire `src/components/PremierLancement.tsx`**

```tsx
import { useState, type FormEvent } from "react";
import { invoke } from "@tauri-apps/api/core";
import { t } from "../i18n/fr";
import { appliquer, type Amorce, type EtatConnexion } from "../lib/amorce";
import { api } from "../lib/api";
import type { UserInfo } from "../hooks/useStaticData";

type Etape =
  | { phase: "saisie"; erreur: string | null }
  | { phase: "verification" }
  | { phase: "valide"; compte: UserInfo };

/**
 * Premier lancement (spec §6). L'ordre est contraint : `get_user` ne peut
 * répondre qu'à travers un sidecar DÉJÀ lancé avec ce jeton — on enregistre,
 * on lance, puis on interroge, et seulement alors on annonce le compte.
 */
export function PremierLancement({ onPret }: { onPret: (a: Amorce) => void }) {
  const [jeton, setJeton] = useState("");
  const [etape, setEtape] = useState<Etape>({ phase: "saisie", erreur: null });
  const occupe = etape.phase === "verification";

  async function valider(e: FormEvent) {
    e.preventDefault();
    // Garde de double envoi : deux validations rapides lanceraient deux
    // sidecars (même piège que le Composer).
    if (occupe || jeton.trim() === "") return;
    setEtape({ phase: "verification" });
    try {
      const etat = await invoke<EtatConnexion>("enregistrer_jeton", {
        jetonRaindrop: jeton.trim(),
      });
      if (etat.kind !== "pret") {
        // Node absent ou panne : ce n'est pas le jeton qui est en cause,
        // cet écran n'a rien à en dire — il remonte.
        onPret(appliquer(etat));
        return;
      }
      appliquer(etat); // pose window.RAINDROP_GUI : l'appel suivant peut aboutir
      setEtape({ phase: "valide", compte: await api.get<UserInfo>("/api/user") });
    } catch (err) {
      // Ne pas laisser une connexion à demi ouverte : le sidecar tourne avec
      // un jeton que Raindrop refuse, l'application ne lirait rien.
      delete window.RAINDROP_GUI;
      setEtape({
        phase: "saisie",
        erreur: err instanceof Error ? err.message : String(err),
      });
    }
  }

  return (
    <main className="mx-auto flex h-screen max-w-xl flex-col justify-center gap-4 px-6">
      <h1 className="text-lg font-medium">{t("boot.title")}</h1>
      <p className="text-sm text-app-muted">{t("boot.explain")}</p>

      {etape.phase === "valide" ? (
        <>
          <p role="status" className="text-sm">
            {t("boot.account", {
              name: etape.compte.fullName,
              email: etape.compte.email,
              count: etape.compte.bookmarksCount,
            })}
          </p>
          <button type="button" className="btn self-start" onClick={() => onPret({ ecran: "app" })}>
            {t("boot.enter")}
          </button>
        </>
      ) : (
        <form onSubmit={valider} className="flex flex-col gap-3">
          <label className="flex flex-col gap-1 text-sm">
            {t("boot.tokenLabel")}
            <input
              type="password"
              className="input"
              autoFocus
              value={jeton}
              onChange={(e) => setJeton(e.target.value)}
            />
          </label>
          <p className="url text-xs text-app-muted">{t("boot.where")}</p>
          <button type="submit" className="btn self-start" disabled={occupe || jeton.trim() === ""}>
            {occupe ? t("boot.checking") : t("boot.validate")}
          </button>
          {etape.phase === "saisie" && etape.erreur !== null && (
            <p role="alert" className="text-xs text-app-broken">
              {t("state.error", { message: etape.erreur })}
            </p>
          )}
        </form>
      )}
    </main>
  );
}
```

- [ ] **Step 5: Brancher l'écran dans `src/main.tsx`**

```tsx
// Import à ajouter : import { PremierLancement } from "./components/PremierLancement";
function Racine({ amorce }: { amorce: Amorce }) {
  const [etat, setEtat] = useState(amorce);
  if (etat.ecran === "premier-lancement") return <PremierLancement onPret={setEtat} />;
  if (etat.ecran !== "app") return <p className="p-6">{etat.detail}</p>; // Task 10
  return <App />;
}
```

- [ ] **Step 6: Lancer les tests et vérifier qu'ils passent**

```bash
npx vitest run src/components/PremierLancement.test.tsx 2>&1 | tail -6
npm run typecheck:front
```

Attendu : `Tests  6 passed (6)`, typecheck sans erreur.

- [ ] **Step 7: Vérification manuelle du vrai premier lancement**

Retirer temporairement le jeton du trousseau, lancer, saisir, remettre.

```bash
security find-generic-password -s raindrop-api-token -a "$USER" -w > /tmp/jeton-sauve
security delete-generic-password -s raindrop-api-token -a "$USER"
npm run tauri:dev
```

Attendu : l'écran « Connecter votre compte Raindrop » s'ouvre. Coller le jeton
(`cat /tmp/jeton-sauve`), cliquer « Valider ». Attendu : « Compte détecté :
… — 12210 signets » puis, après « Ouvrir la bibliothèque », la bibliothèque.
Quitter, puis **effacer la copie en clair** :

```bash
shred -u /tmp/jeton-sauve 2>/dev/null || rm -P /tmp/jeton-sauve
security find-generic-password -s raindrop-api-token -a "$USER" -w | head -c 8; echo "…"
```

Attendu : le jeton est de retour au trousseau (écrit par l'application), et
la copie temporaire n'existe plus.

- [ ] **Step 8: Commit**

```bash
git add src/components/PremierLancement.tsx src/components/PremierLancement.test.tsx src/i18n/fr.ts src/main.tsx
git commit -F - <<'EOF'
feat(front): écran de premier lancement

Plan 3/3, Task 9 (spec §6). L'ordre est contraint par les faits :
get_user ne peut répondre qu'à travers un sidecar DÉJÀ lancé avec ce
jeton. La séquence est donc enregistrer → lancer → interroger /api/user →
annoncer le compte → entrer.

Deux gardes qui comptent : une double validation rapide ne lance pas deux
sidecars, et un jeton refusé efface window.RAINDROP_GUI — sinon
l'application s'ouvrirait sur une connexion incapable de rien lire.

Un « node absent » remonte au lieu de rester sur cet écran : ce n'est pas
le jeton qui est en cause, et cet écran n'a rien à en dire.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 10: Les écrans de diagnostic et de panne — avec une issue

**Files:**
- Create: `src/components/EcranAmorce.tsx`
- Create: `src/components/EcranAmorce.test.tsx`
- Modify: `src/main.tsx`

**Interfaces:**
- Consumes: Task 8 (`Amorce`, `relancer`), Task 9 (clés i18n `boot.*`).
- Produces, exportés de `EcranAmorce.tsx` :
  - `<Diagnostic detail={string} onEtat={(a: Amorce) => void} />`
  - `<EcranPanne detail={string} onEtat={(a: Amorce) => void} />`

**Spec §7 :** « Node absent du PATH → écran de diagnostic au lancement avec
instructions ». Un écran qui dit seulement « erreur » ne remplit pas ce
contrat : il doit porter **ce qui a été constaté** et **quoi faire**.

**Le piège, et pourquoi ces écrans ont des boutons.** Sans issue, l'écran de
panne **enferme** : `enregistrer_jeton` écrit le jeton au trousseau AVANT de
lancer le sidecar, donc un jeton refusé — ou une panne passagère — laisse un
jeton en place ; au lancement suivant, la séquence le relit, échoue de la même
façon, et retombe sur le même écran. Il n'y a pas d'écran de réglages
(délibérément hors périmètre) : sans bouton ici, **rien** ne ramène à la
saisie. C'est la même règle que partout ailleurs dans ce dépôt — une erreur
se dit **et** offre une sortie (`EtatListe`, bannière du sidecar injoignable).

Le « Réessayer » passe par `relancer()`, pas `amorcer()` : la seconde rendrait
l'état mémorisé, donc la même panne, indéfiniment.

- [ ] **Step 1: Ajouter les deux textes manquants**

Dans `src/i18n/fr.ts`, à la suite des clés `boot.*` :

```ts
  "boot.retrySaisie": "Saisir un autre jeton",
```

`state.retry` (« Réessayer ») existe déjà — la réutiliser.

- [ ] **Step 2: Écrire les tests qui échouent**

`src/components/EcranAmorce.test.tsx` :

```tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { Diagnostic, EcranPanne } from "./EcranAmorce";

const { relancerMock } = vi.hoisted(() => ({ relancerMock: vi.fn() }));
vi.mock("../lib/amorce", async (vrai) => ({
  ...(await vrai<object>()),
  relancer: relancerMock,
}));

beforeEach(() => {
  relancerMock.mockReset().mockResolvedValue({ ecran: "app" });
});

describe("Diagnostic", () => {
  // Le détail vient de Rust (node::Verdict) et porte la version trouvée :
  // le perdre renverrait l'utilisateur installer ce qu'il a déjà.
  it("montre le constat de Rust ET l'instruction", () => {
    render(
      <Diagnostic
        detail="Node v18.19.0 trouvé (/usr/bin/node), mais la version 20 ou supérieure est requise."
        onEtat={vi.fn()}
      />,
    );
    expect(screen.getByRole("heading", { name: "Node est introuvable" })).toBeInTheDocument();
    expect(screen.getByText(/v18\.19\.0/)).toBeInTheDocument();
    expect(screen.getByText(/brew install node/)).toBeInTheDocument();
  });

  it("« Réessayer » REJOUE la séquence et remonte le nouvel état", async () => {
    const onEtat = vi.fn();
    render(<Diagnostic detail="rien trouvé" onEtat={onEtat} />);
    await userEvent.click(screen.getByRole("button", { name: "Réessayer" }));
    expect(relancerMock).toHaveBeenCalled();
    await waitFor(() => expect(onEtat).toHaveBeenCalledWith({ ecran: "app" }));
  });
});

describe("EcranPanne", () => {
  it("montre le détail et où regarder", () => {
    render(<EcranPanne detail="le sidecar n'a pas publié de port" onEtat={vi.fn()} />);
    expect(screen.getByRole("heading", { name: "Le service local n'a pas démarré" })).toBeInTheDocument();
    expect(screen.getByText(/n'a pas publié de port/)).toBeInTheDocument();
    expect(screen.getByText(/journal de démarrage/)).toBeInTheDocument();
  });

  it("le détail est annoncé comme une alerte", () => {
    render(<EcranPanne detail="boum" onEtat={vi.fn()} />);
    expect(screen.getByRole("alert")).toHaveTextContent("boum");
  });

  // LE test qui compte. Sans cette porte, un jeton refusé enferme pour de
  // bon : il est déjà au trousseau, la séquence le relit à chaque lancement
  // et échoue pareil, et aucun écran de réglages n'existe pour le changer.
  it("offre de saisir un autre jeton — sinon l'écran est un cul-de-sac", async () => {
    const onEtat = vi.fn();
    render(<EcranPanne detail="jeton refusé" onEtat={onEtat} />);
    await userEvent.click(screen.getByRole("button", { name: "Saisir un autre jeton" }));
    expect(onEtat).toHaveBeenCalledWith({ ecran: "premier-lancement" });
  });

  it("offre aussi de réessayer", async () => {
    render(<EcranPanne detail="panne passagère" onEtat={vi.fn()} />);
    await userEvent.click(screen.getByRole("button", { name: "Réessayer" }));
    expect(relancerMock).toHaveBeenCalled();
  });
});
```

- [ ] **Step 3: Lancer les tests et vérifier qu'ils échouent**

```bash
npx vitest run src/components/EcranAmorce.test.tsx 2>&1 | tail -6
```

Attendu : ÉCHEC — `Failed to resolve import "./EcranAmorce"`.

- [ ] **Step 4: Écrire `src/components/EcranAmorce.tsx`**

```tsx
import { useState, type ReactNode } from "react";
import { t } from "../i18n/fr";
import { relancer, type Amorce } from "../lib/amorce";

// Les deux écrans qui disent pourquoi l'application ne peut PAS s'ouvrir.
// Même forme : un constat (venu de Rust), une instruction, et AU MOINS UNE
// ISSUE — sans elle, un jeton refusé enfermerait pour de bon (il est déjà au
// trousseau, la séquence le relit à chaque lancement) et aucun écran de
// réglages n'existe pour le changer.
function Ecran({
  titre,
  detail,
  aide,
  actions,
}: {
  titre: string;
  detail: string;
  aide: ReactNode;
  actions: ReactNode;
}) {
  return (
    <main className="mx-auto flex h-screen max-w-xl flex-col justify-center gap-4 px-6">
      <h1 className="text-lg font-medium">{titre}</h1>
      <p role="alert" className="text-sm text-app-broken">
        {detail}
      </p>
      <p className="text-sm text-app-muted">{aide}</p>
      <div className="flex gap-2">{actions}</div>
    </main>
  );
}

/** « Réessayer » passe par `relancer` : `amorcer` rendrait l'état mémorisé,
 *  donc la même panne, indéfiniment. */
function BoutonReessayer({ onEtat }: { onEtat: (a: Amorce) => void }) {
  const [occupe, setOccupe] = useState(false);
  return (
    <button
      type="button"
      className="btn"
      disabled={occupe}
      onClick={() => {
        setOccupe(true);
        void relancer().then((a) => {
          setOccupe(false);
          onEtat(a);
        });
      }}
    >
      {occupe ? t("boot.checking") : t("state.retry")}
    </button>
  );
}

export function Diagnostic({ detail, onEtat }: { detail: string; onEtat: (a: Amorce) => void }) {
  return (
    <Ecran
      titre={t("boot.nodeTitle")}
      detail={detail}
      aide={t("boot.nodeHelp")}
      actions={<BoutonReessayer onEtat={onEtat} />}
    />
  );
}

export function EcranPanne({ detail, onEtat }: { detail: string; onEtat: (a: Amorce) => void }) {
  return (
    <Ecran
      titre={t("boot.panneTitle")}
      detail={detail}
      aide={t("boot.panneHelp")}
      actions={
        <>
          <BoutonReessayer onEtat={onEtat} />
          {/* La seule porte vers la saisie : sans elle, un jeton refusé —
              déjà écrit au trousseau — enferme à chaque lancement. */}
          <button
            type="button"
            className="btn"
            onClick={() => onEtat({ ecran: "premier-lancement" })}
          >
            {t("boot.retrySaisie")}
          </button>
        </>
      }
    />
  );
}
```

- [ ] **Step 5: Compléter `src/main.tsx`**

```tsx
// Import à ajouter : import { Diagnostic, EcranPanne } from "./components/EcranAmorce";
function Racine({ amorce }: { amorce: Amorce }) {
  const [etat, setEtat] = useState(amorce);
  switch (etat.ecran) {
    case "premier-lancement":
      return <PremierLancement onPret={setEtat} />;
    case "diagnostic":
      return <Diagnostic detail={etat.detail} onEtat={setEtat} />;
    case "panne":
      return <EcranPanne detail={etat.detail} onEtat={setEtat} />;
    default:
      return <App />;
  }
}
```

- [ ] **Step 6: Lancer les tests et vérifier qu'ils passent**

```bash
npx vitest run src/components/EcranAmorce.test.tsx 2>&1 | tail -6
npm run typecheck:front
```

Attendu : `Tests  6 passed (6)`, typecheck sans erreur.

- [ ] **Step 7: Vérification manuelle — l'écran Node**

Simuler l'absence de Node en tronquant le PATH pour le processus Tauri :

```bash
env PATH=/usr/bin:/bin npm run tauri:dev
```

Attendu : **l'application s'ouvre quand même** et affiche « Node est
introuvable » avec la ligne « brew install node » et un bouton « Réessayer ».
(Si la machine a `/usr/local/bin/node`, ce test ne déclenche rien — retirer
aussi ce chemin des candidats, temporairement, pour le vérifier.)

- [ ] **Step 8: Vérification manuelle — sortir d'un jeton refusé**

Le scénario qui motive cette task. **Sauvegarder d'abord le vrai jeton.**

```bash
security find-generic-password -s raindrop-api-token -a "$USER" -w > /tmp/jeton-sauve
security add-generic-password -s raindrop-api-token -a "$USER" -U -w "jeton-invalide-de-test"
npm run tauri:dev
```

Attendu : l'application s'ouvre sur un écran qui **dit** le problème et offre
« Saisir un autre jeton ». Cliquer dessus, coller le vrai jeton
(`cat /tmp/jeton-sauve`), valider : la bibliothèque s'ouvre. Puis nettoyer :

```bash
shred -u /tmp/jeton-sauve 2>/dev/null || rm -P /tmp/jeton-sauve
```

**Si l'écran n'offre aucun bouton, la task est ratée** : l'utilisateur serait
enfermé, sans écran de réglages pour s'en sortir.

- [ ] **Step 9: Commit**

```bash
git add src/components/EcranAmorce.tsx src/components/EcranAmorce.test.tsx src/i18n/fr.ts src/main.tsx
git commit -F - <<'EOF'
feat(front): écrans de diagnostic et de panne, avec une issue

Plan 3/3, Task 10 (spec §7). Un écran qui dit « erreur » ne remplit pas
le contrat : celui-ci porte le CONSTAT venu de Rust — la version de Node
trouvée, le port jamais publié — et l'INSTRUCTION qui va avec.

Surtout, il a des BOUTONS. Sans eux l'écran de panne enfermait :
enregistrer_jeton écrit le jeton au trousseau AVANT de lancer le sidecar,
donc un jeton refusé reste en place ; au lancement suivant la séquence le
relit, échoue pareil, et retombe sur le même écran — et il n'y a pas
d'écran de réglages pour en changer. « Saisir un autre jeton » est la
seule porte.

« Réessayer » passe par la commande `relancer`, qui REJOUE la séquence :
`etat_connexion` rendrait l'état mémorisé, donc la même panne à jamais.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 11: La bannière ne crie pas au démarrage à froid

**Files:**
- Modify: `src/components/Banners.tsx`
- Modify: `src/components/Banners.test.tsx`

**Interfaces:**
- Consumes: rien de neuf.
- Produces: comportement corrigé de `<Banners />`.

**Le grief (noté en ROADMAP) :** au démarrage à froid, le sidecar répond
`mcp: "starting"` pendant quelques secondes. La bannière affiche alors
« Connexion Raindrop interrompue » — or rien n'a été interrompu, la connexion
n'a jamais encore été établie. Le mot est faux, et l'alarme injustifiée.

**La correction précise :** ne pas se taire tant qu'on n'a pas vu
`connected` — ce serait taire aussi un `crashed` franc, qui lui est
sans ambiguïté dès la première réponse. La règle est donc : la bannière parle
si l'on a **déjà vu** `connected`, **ou** si l'état est explicitement
`crashed`.

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à `src/components/Banners.test.tsx` :

```tsx
  it("au démarrage à froid, « starting » ne déclenche aucune alarme", () => {
    // Rien n'a été « interrompu » : la connexion n'a jamais été établie.
    healthMock.mockReset().mockReturnValue({
      data: { status: "ok", mcp: "starting" }, isError: false, refetch: vi.fn(),
    });
    const { container } = render(<Banners />, { wrapper });
    expect(container).toBeEmptyDOMElement();
  });

  it("un « crashed » franc parle dès la première réponse", () => {
    // Pas d'ambiguïté possible : inutile d'attendre d'avoir vu connected.
    healthMock.mockReset().mockReturnValue({
      data: { status: "ok", mcp: "crashed" }, isError: false, refetch: vi.fn(),
    });
    render(<Banners />, { wrapper });
    expect(screen.getByRole("alert")).toHaveTextContent(/interrompue/);
  });

  it("après avoir vu connected, « restarting » parle", () => {
    const refetch = vi.fn();
    healthMock.mockReset().mockReturnValue({
      data: { status: "ok", mcp: "connected" }, isError: false, refetch,
    });
    const { rerender } = render(<Banners />, { wrapper });
    expect(screen.queryByRole("alert")).not.toBeInTheDocument();
    healthMock.mockReturnValue({
      data: { status: "ok", mcp: "restarting" }, isError: false, refetch,
    });
    rerender(<Banners />);
    expect(screen.getByRole("alert")).toHaveTextContent(/interrompue/);
  });
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

```bash
npx vitest run src/components/Banners.test.tsx 2>&1 | tail -10
```

Attendu : ÉCHEC du premier test — la bannière s'affiche sur `starting`
(`expected <div /> to be empty`).

- [ ] **Step 3: Écrire l'implémentation**

Dans `src/components/Banners.tsx`, remplacer le calcul de `mcpEnRade` :

```tsx
  // Au démarrage à froid le sidecar répond « starting » quelques secondes :
  // annoncer « interrompue » y serait faux — rien n'a été interrompu, la
  // connexion n'a jamais encore été établie. On attend donc d'avoir vu
  // `connected` une fois... SAUF pour un « crashed » franc, sans ambiguïté
  // dès la première réponse (sinon un sidecar qui ne se connecte jamais
  // resterait muet pour toujours).
  const [dejaConnecte, setDejaConnecte] = useState(false);
  useEffect(() => {
    if (data?.mcp === "connected") setDejaConnecte(true);
  }, [data?.mcp]);
  const mcpEnRade =
    data !== undefined &&
    data.mcp !== "connected" &&
    (dejaConnecte || data.mcp === "crashed");
```

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

```bash
npx vitest run src/components/Banners.test.tsx 2>&1 | tail -6
```

Attendu : tous les tests de `Banners` passent, les anciens compris.

- [ ] **Step 5: Vérifier l'autre note de la ROADMAP — la prévisualisation Vite**

Le proxy ne doit pas être exigé par `vite preview` (il n'y a pas de sidecar) :

```bash
npm run build:front && npm run preview &
sleep 3
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/
kill %1
```

Attendu : `200`. Le garde `!isPreview` de `vite.config.ts` couvre déjà ce cas
— ce step le **vérifie**, il n'y a rien à changer si le code répond.

- [ ] **Step 6: Suite complète**

```bash
npm test 2>&1 | grep -E "Tests |FAIL"
npm run typecheck && npm run typecheck:front
cd src-tauri && cargo test 2>&1 | tail -4
```

Attendu : aucun échec des trois côtés.

- [ ] **Step 7: Commit**

```bash
git add src/components/Banners.tsx src/components/Banners.test.tsx
git commit -F - <<'EOF'
fix(front): la bannière ne crie plus au démarrage à froid

Plan 3/3, Task 11. Le sidecar répond « starting » quelques secondes au
lancement, et la bannière annonçait « Connexion Raindrop interrompue » —
or rien n'avait été interrompu : la connexion n'avait jamais été établie.
Le mot était faux et l'alarme injustifiée.

La correction n'est pas « se taire tant qu'on n'a pas vu connected » :
cela tairait aussi un « crashed » franc, sans ambiguïté dès la première
réponse, et un sidecar qui ne se connecte jamais resterait muet pour
toujours. La règle est donc : parler si l'on a DÉJÀ VU connected, ou si
l'état est explicitement crashed.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 12: L'empaquetage — un `.app` qui marche hors du dépôt

**Files:**
- Create: `scripts/preparer-ressources.sh`
- Modify: `src-tauri/tauri.conf.json` (`bundle.resources`, `beforeBuildCommand`)
- Modify: `.gitignore` (`src-tauri/ressources/`)
- Modify: `docs/ROADMAP.md`, `CHANGELOG.md`

**Interfaces:**
- Consumes: toutes les tasks précédentes. En particulier `lib.rs::base()`, qui
  en production rend `resource_dir()/ressources`.
- Produces: `Raindrop GUI.app` et un `.dmg`.

**Ce qui doit voyager dans le `.app` :** le sidecar compilé
(`dist-sidecar/`) **et** ses dépendances d'exécution (`node_modules/` de
production : `hono`, `@hono/node-server`, `zod`,
`@modelcontextprotocol/sdk`, et `@kud/mcp-raindrop-io` épinglé à 1.3.1). Sans
elles, le sidecar démarre et meurt sur le premier `import`.

**Ce qui ne voyage pas :** Node lui-même. La spec §3.2 l'assume — « le sidecar
et le serveur MCP nécessitent Node ≥ 20 présent dans le PATH », la compilation
en binaire autonome étant repoussée en Phase 2. C'est ce que couvrent la
Task 3 (résolution) et la Task 10 (écran de diagnostic).

**Vérifié le 2026-09-17 :** `tauri build` produit `.app` **et** `.dmg` avec
les seuls Command Line Tools, en 1 min 31 s. La signature est **ad-hoc**
(`adhoc, linker-signed`, `TeamIdentifier=not set`) : pas de Developer ID,
donc Gatekeeper met l'application en quarantaine au premier lancement. Le
Step 6 le traite.

- [ ] **Step 1: Écrire `scripts/preparer-ressources.sh`**

```bash
#!/usr/bin/env bash
# Assemble ce que le .app doit emporter : le sidecar compilé et ses
# dépendances d'EXÉCUTION seulement.
#
# Pourquoi une installation à part plutôt qu'une copie de node_modules/ :
# l'arbre de développement porte Vite, Vitest, TypeScript et Playwright —
# des centaines de mégaoctets qui n'ont rien à faire dans l'application.
set -euo pipefail
cd "$(dirname "$0")/.."

RES="src-tauri/ressources"
ETAPE="$(mktemp -d)"
trap 'rm -rf "$ETAPE"' EXIT

echo "[ressources] compilation du sidecar"
npm run build:sidecar

echo "[ressources] dépendances de production"
cp package.json package-lock.json "$ETAPE/"
( cd "$ETAPE" && npm ci --omit=dev --ignore-scripts >/dev/null )

echo "[ressources] assemblage"
rm -rf "$RES"
mkdir -p "$RES"
cp -R dist-sidecar "$RES/dist-sidecar"
cp -R "$ETAPE/node_modules" "$RES/node_modules"

# Contrôle : le paquet MCP épinglé DOIT être là, et à la bonne version.
VERSION=$(python3 -c "import json;print(json.load(open('$RES/node_modules/@kud/mcp-raindrop-io/package.json'))['version'])")
[ "$VERSION" = "1.3.1" ] || { echo "MCP attendu en 1.3.1, trouvé $VERSION" >&2; exit 1; }
[ -f "$RES/dist-sidecar/sidecar/index.js" ] || { echo "entrée du sidecar absente" >&2; exit 1; }

echo "[ressources] prêt — $(du -sh "$RES" | cut -f1)"
```

Le rendre exécutable :

```bash
chmod +x scripts/preparer-ressources.sh
```

- [ ] **Step 2: Lancer le script et vérifier son résultat**

```bash
./scripts/preparer-ressources.sh
```

Attendu : les trois lignes d'étape, puis `[ressources] prêt — <taille>`. Si
le contrôle de version échoue, c'est que `package.json` a dérivé de
l'épinglage 1.3.1 — le corriger plutôt que le contourner (CLAUDE.md).

- [ ] **Step 3: Déclarer les ressources dans `src-tauri/tauri.conf.json`**

```json
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "npm run build:front && ./scripts/preparer-ressources.sh",
    "frontendDist": "../dist"
  },
```

et, dans `bundle` :

```json
    "resources": ["ressources/**/*"],
```

Les ressources gardent leur chemin relatif dans le paquet : elles atterrissent
en `<resource_dir>/ressources/…`, ce que `lib.rs::base()` va chercher.

- [ ] **Step 4: Ignorer les ressources assemblées**

```bash
grep -q "src-tauri/ressources" .gitignore || cat >> .gitignore <<'EOF'

# Ressources assemblées pour le .app (régénérées par scripts/preparer-ressources.sh)
src-tauri/ressources/
EOF
```

- [ ] **Step 5: Construire**

```bash
npm run tauri:build 2>&1 | tail -12
```

Attendu : `Finished 2 bundles at:` suivi des chemins du `.app` et du `.dmg`
sous `src-tauri/target/release/bundle/`. Compter plusieurs minutes.

- [ ] **Step 6: Vérification manuelle — l'application marche hors du dépôt**

C'est **le** test de cette task : tout ce qui manquerait ne se voit qu'ici.

```bash
APP="src-tauri/target/release/bundle/macos/Raindrop GUI.app"
# Gatekeeper : la signature est ad-hoc, donc l'app est mise en quarantaine.
xattr -dr com.apple.quarantine "$APP" 2>/dev/null || true
cp -R "$APP" /tmp/
```

**a) Le lancement réaliste**, celui du Finder :

```bash
open -W "/tmp/Raindrop GUI.app"
```

**b) Le lancement à environnement contraint** — c'est LUI qui prouve la
Task 3. `open` délègue à launchd, qui **ne transmet pas** l'environnement
qu'on lui donne : `env -i … open` passerait sans avoir rien contraint du tout,
et l'on croirait avoir vérifié la résolution de `node` sans l'avoir exercée.
Il faut appeler le binaire **directement** :

```bash
env -i HOME="$HOME" USER="$USER" PATH=/usr/bin:/bin   "/tmp/Raindrop GUI.app/Contents/MacOS/Raindrop GUI"
```

Attendu, **dans les deux cas** : la fenêtre s'ouvre et **la bibliothèque se
charge** — collections dans la barre latérale, signets dans la liste. Si
l'écran « Node est introuvable » paraît au (b), c'est que la Task 3 n'a pas
fait son travail : le PATH est volontairement minimal, et la résolution doit
trouver `/opt/homebrew/bin/node` par ses candidats.

**c) Le même, sans `USER`** — le cas launchd que la Task 5 couvre :

```bash
env -i HOME="$HOME" PATH=/usr/bin:/bin   "/tmp/Raindrop GUI.app/Contents/MacOS/Raindrop GUI"
```

Attendu : la bibliothèque se charge encore. Si l'écran de **premier
lancement** paraît alors que le jeton est bien au trousseau, c'est le repli de
`compte()` qui manque : sans `USER`, le compte serait vide et l'entrée
introuvable.

**Note sur le trousseau :** au premier accès depuis l'application empaquetée,
macOS demande une autorisation. La signature étant **ad-hoc et différente à
chaque construction**, « Toujours autoriser » ne tient pas d'un build à
l'autre — la demande revient. Elle peut apparaître **derrière** la fenêtre :
ne pas la prendre pour un gel.

Vérifier ensuite que le sidecar a bien été lancé **depuis les ressources du
paquet**, et non depuis le dépôt :

```bash
pgrep -fl "Raindrop GUI.app" | head -3
```

Attendu : une ligne citant `…/Raindrop GUI.app/Contents/Resources/ressources/dist-sidecar/sidecar/index.js`.

Quitter l'application, puis nettoyer :

```bash
pkill -f "Raindrop GUI" 2>/dev/null; rm -rf "/tmp/Raindrop GUI.app"
pgrep -f "dist-sidecar/sidecar/index.js" || echo "aucun sidecar résiduel"
```

Attendu : `aucun sidecar résiduel` — l'arrêt propre de la Task 7 tient aussi
dans l'application empaquetée.

- [ ] **Step 7: Mettre la documentation à jour**

Dans `docs/ROADMAP.md`, fermer l'entrée du plan 3 et y consigner ce qui reste
sciemment dehors :

```markdown
- [x] **Plan 3/3 — shell Tauri** — fait le 2026-09-17. Fenêtre, spawn et
      arrêt du sidecar, token Bearer local éphémère, token Raindrop au
      trousseau macOS, écran de premier lancement, écran de diagnostic
      Node, empaquetage `.app` + `.dmg`.
      **Reste dehors, à dessein** : la signature est **ad-hoc** (pas de
      Developer ID) — Gatekeeper met l'application en quarantaine, à lever
      une fois par `xattr -dr com.apple.quarantine` ; l'**icône** est un
      carré uni provisoire (l'icône définitive relève de DESIGN.md) ; il
      n'y a **pas d'écran de réglages** (spec §6 en mentionne un pour
      remplacer le token — la commande `enregistrer_jeton` le permet déjà,
      l'écran reste à faire) ; **Node n'est pas embarqué** (spec §3.2, la
      compilation en binaire autonome est repoussée en Phase 2).
      Le **sélecteur de dossier de sauvegarde** que cette entrée portait
      est déplacé : il dépend de la spec sauvegarde, non validée.
```

Dans `CHANGELOG.md`, sous `## [Unreleased]`, remplacer le paragraphe
d'ouverture (« Le **plan 3/3 — le shell Tauri — n'est pas commencé** ») par
une phrase qui dit l'état réel, et ajouter une section :

```markdown
#### Application macOS — plan 3/3 (2026-09-17)

- **Raindrop GUI est une application.** Une fenêtre s'ouvre, lance son
  service local, et l'arrête en partant.
- **Premier lancement** : le jeton d'API se saisit une fois, est vérifié
  auprès de Raindrop — le compte détecté s'affiche — et rejoint le
  **trousseau macOS**. Il ne quitte jamais cet ordinateur.
- **Si Node manque**, l'application le dit et explique quoi installer, au
  lieu de s'ouvrir sur une bibliothèque vide.
- Livrée en `.app` et en `.dmg`. La signature étant ad-hoc, macOS demande
  une confirmation au premier lancement.
```

- [ ] **Step 8: Commit**

```bash
git add scripts/preparer-ressources.sh src-tauri/tauri.conf.json .gitignore docs/ROADMAP.md CHANGELOG.md
git commit -F - <<'EOF'
feat(tauri): empaquetage — un .app qui marche hors du dépôt

Plan 3/3, Task 12. Le paquet emporte le sidecar compilé ET ses
dépendances d'exécution, installées à part (`npm ci --omit=dev`) plutôt
que copiées du dépôt : l'arbre de développement porte Vite, Vitest,
TypeScript et Playwright, qui n'ont rien à faire dans l'application.

Le script refuse d'assembler si le MCP n'est pas en 1.3.1 ou si l'entrée
du sidecar manque — deux pannes qui, sinon, ne se verraient qu'au premier
lancement de l'app livrée.

Node n'est pas embarqué : la spec §3.2 l'assume et repousse le binaire
autonome en Phase 2. C'est ce que couvrent la résolution de node et
l'écran de diagnostic.

Vérifié en lançant l'app copiée hors du dépôt, avec un PATH minimal comme
celui du Finder.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Auto-relecture

Faite après écriture, contre la spec.

### Couverture de la spec

| Exigence | Task |
|---|---|
| §2 « Shell Tauri (Rust minimal) — fenêtre » | 1 |
| §2 « spawn/arrêt du sidecar » | 6, 7 |
| §2 « transmission du port+token local au webview » | 7, 8 |
| §2 « accès au trousseau macOS » | 5 |
| §3.1 MCP épinglé, spawn direct du JS | 6 (`entree_mcp`), 12 (contrôle de version) |
| §3.2 Node ≥ 20 vérifié, erreur claire | 3, 10 |
| §3.6 lockfile `{port, pid}`, `port: 0` = binding | 4, 6 |
| §3.6 réutilisation / doublons | 4 (décision D2, écart assumé et motivé) |
| §3.6 « crash = notification + bouton Redémarrer » | déjà livré (`Banners`), corrigé en 11 |
| §3.7 token éphémère généré par Tauri, jamais en clair sur disque | 2, 6, 7 |
| §3.7 `Origin` vérifié, CORS minimal | déjà livré (`sidecar/api/app.ts`) — non régressé |
| §6 stockage au trousseau, repli documenté | 5 (le repli `config.json` **n'est pas** implémenté — voir « Écarts » ci-dessous) |
| §6 premier lancement : saisie, validation par `get_user`, démarrage | 9 |
| §6 transmission par `MCP_RAINDROPIO_TOKEN`, jamais par HTTP | 6 |
| §6 réglages : remplacer le token | **partiel** — la commande existe (7, 9), l'écran non (voir « Écarts ») |
| §7 Node absent → écran de diagnostic | 3, 10 |
| §7 port/lockfile corrompu → nouveau bind | 4 (`lire` rend `None`), 7 (effacement) |
| §8 pas d'E2E Tauri en Phase 1 | respecté : pur testé, impur vérifié à la main |

### Écarts assumés, à consigner en ROADMAP

1. **Repli `config.json` chmod 600** si le trousseau est indisponible (§6) :
   non implémenté. Le trousseau macOS est disponible sur toute machine cible ;
   coder un repli non testable ajouterait un chemin secret sur disque — ce que
   §3.7 cherche précisément à éviter. `trousseau::lire` rend une erreur
   explicite, qui devient un écran de panne.
2. **Écran de réglages** (§6) : la commande `enregistrer_jeton` remplace un
   token à tout moment, mais aucun écran ne l'appelle hors du premier
   lancement. À faire dans son propre lot.
3. **Sélecteur du dossier de sauvegarde** : retiré (spec non validée).

### Corrections issues de la relecture

Cinq défauts trouvés en relisant le plan écrit, corrigés dans les tasks :

1. **L'écran de panne était un cul-de-sac** (bloquant). `enregistrer_jeton`
   écrit le jeton au trousseau AVANT de lancer le sidecar : un jeton refusé
   restait en place, la séquence le relisait à chaque lancement, et l'écran
   sans bouton n'offrait aucune sortie — sans écran de réglages pour en
   changer. Task 10 réécrite : « Réessayer » et « Saisir un autre jeton ».
2. **Un « Réessayer » naïf n'aurait rien réessayé** : `etat_connexion` rend
   l'état MÉMORISÉ. D'où la commande `relancer` (Task 7) et `relancer()`
   (Task 8).
3. **Le webview atteint-il `127.0.0.1` ?** Jamais exercé — les plans 1 et 2
   passaient par le proxy Vite, en même origine. Contrôle ajouté en Task 1
   (Step 12 bis), avec ordre d'arrêter le plan si l'appel est refusé.
4. **`open -W` ne transmet pas l'environnement** (launchd décide) : la
   vérification du PATH contraint de la Task 12 aurait passé sans rien
   contraindre. Elle appelle désormais le binaire directement.
5. **`compte()` rendait `""` sans `USER`** — le cas d'une application lancée
   par launchd. Symptôme : l'écran de premier lancement à chaque ouverture,
   jeton pourtant au trousseau. Repli sur le dossier personnel, et test non
   tautologique (Task 5).

### Cohérence des types entre tasks

- `EtatConnexion` : défini en Task 7 (Rust, `#[serde(tag = "kind",
  rename_all = "snake_case")]`), miroir TypeScript en Task 8, consommé en
  Tasks 9 et 10. Les quatre `kind` — `pret`, `jeton_requis`, `node_absent`,
  `panne` — sont testés des deux côtés.
- `node::Verdict` : produit en Task 3, consommé en Task 7 (`verdict_en_etat`).
- `verrou::Verrou` / `Decision` : produits en Task 4, consommés en Task 7.
- `sidecar::Reglages` : champs `node`, `base`, `token_raindrop`,
  `token_local`, `dossier_donnees` — mêmes noms en Tasks 6 et 7.
- `Amorce` : produit en Task 8, consommé en Tasks 9, 10 et `main.tsx`.
  Attention aux deux noms de prop : `PremierLancement` reçoit `onPret`,
  `Diagnostic` et `EcranPanne` reçoivent `onEtat` — tous trois de type
  `(a: Amorce) => void`, tous trois câblés sur le `setEtat` de `Racine`.
- `entree(base)` = `base/dist-sidecar/sidecar/index.js` : le même `base` est
  rendu par `lib.rs::base()` (Task 7) et rempli par
  `scripts/preparer-ressources.sh` (Task 12).

### Task 13: Le runtime Node géré — installation au premier lancement (ajoutée le 2026-09-17, décision utilisateur)

**Files:**
- Create: `src-tauri/src/runtime.rs`
- Modify: `src-tauri/src/node.rs` (résolution : runtime géré en tête)
- Modify: `src-tauri/src/commandes.rs` (commande `installer_runtime`, progression)
- Modify: `src-tauri/src/etat_connexion.rs` (champ de progression partagé)
- Modify: `src-tauri/Cargo.toml` (`sha2` pour la vérification)
- Modify: `src/components/EcranAmorce.tsx` (le bouton principal devient « Installer Node »)
- Modify: `src/lib/amorce.ts` (commande + progression)
- Modify: `src/i18n/fr.ts`
- Modify: `src/components/EcranAmorce.test.tsx`, `src/lib/amorce.test.ts`

**Interfaces:**
- Produces:
  - `runtime::VERSION_PINNEE` (LTS Node 22, version RÉELLE vérifiée par
    `curl -sI https://nodejs.org/dist/<v>/` avant de finaliser),
    `runtime::installer(base: &Path, arch: &str, progresser: impl Fn(String)) -> Result<PathBuf, String>`
  - Chemin du runtime géré : `<dossier_donnees>/runtime/<version>/bin/node`
  - `node::candidats_systeme()` : le runtime géré, s'il existe, passe en
    TÊTE de liste (déterminisme : un Node installé par l'app bat toujours le
    PATH).
  - Commande `installer_runtime() -> EtatConnexion` : installe PUIS rejoue
    `sequence()` (même chemin que `relancer`).
  - Commande `progression_installation() -> Option<String>` (poll du front).

**Décisions de conception (tranchées, ne pas rouvrir) :**
1. **L'installation n'est PAS dans `sequence()`** : la séquence reste rapide
   et pure ; c'est l'écran `NodeAbsent` qui porte le bouton principal
   « Installer Node ». Une fenêtre blanche de 60 s (auto-install muette dans
   la séquence) serait pire qu'un bouton qui rend l'action visible — et le
   téléchargement de 25 Mo mérite un geste de consentement.
2. **Sources : curl et tar du système macOS** (binaires de base, zéro
   dépendance HTTP Rust) ; **sha2 crate** pour la vérification SHASUMS256
   — un contrôle de sécurité ne se délègue pas à un binaire qui peut être
   absent (`/usr/bin/shasum` dépend du CLT).
3. **Pas de droits administrateur** : extraction dans le dossier de données
   de l'app, pas `/usr/local`.
4. **Vérification obligatoire** : sha256 du tarball contre la ligne
   correspondante de SHASUMS256.txt officiel — échec = installation refusée,
   écran en panne avec les instructions manuelles.
5. **Hors-ligne** : échec lisible (`curl` rend une erreur), repli = les
   instructions manuelles de l'écran (déjà là, Task 10).

**Steps (TDD sur les parties pures) :** tests de `ressources_depuis_exe`-style
pour la dérivation de chemins (nom de tarball par archéologie : `aarch64` →
`arm64`, `x86_64` → `x64`), du parsing de SHASUMS256.txt (trouver la ligne du
tarball, rejeter un fichier dont la somme diffère), de l'ordre de résolution
(runtime géré en tête). L'impur (curl, tar, écriture disque) est vérifié par
le contrôleur : installation réelle puis lancement de l'app sans node sur le
PATH. Progression : champ partagé `Mutex<Option<String>>` + commande de poll —
pas d'événements Tauri (le front poll déjà `etat_connexion`, même modèle).
