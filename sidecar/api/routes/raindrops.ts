import { Hono } from "hono";
import type { SidecarDeps } from "../deps.js";

// Stub Task 7 — remplacé par la Task 8 (routes raindrops + mapper DTO).
export const raindropsRoutes = (deps: SidecarDeps): Hono => new Hono();
