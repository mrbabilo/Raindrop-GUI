import { Hono } from "hono";
import type { SidecarDeps } from "../deps.js";

// Stub Task 7 — remplacé par la Task 14 (routes analysis).
export const analysisRoutes = (deps: SidecarDeps): Hono => new Hono();
