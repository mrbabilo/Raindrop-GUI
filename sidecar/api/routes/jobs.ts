import { Hono } from "hono";
import type { SidecarDeps } from "../deps.js";

// Stub Task 7 — remplacé par la Task 10 (routes jobs + SSE).
export const jobsRoutes = (deps: SidecarDeps): Hono => new Hono();
