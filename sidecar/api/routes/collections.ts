import { Hono } from "hono";
import type { SidecarDeps } from "../deps.js";

// Stub Task 7 — remplacé par la Task 9 (routes collections).
export const collectionsRoutes = (deps: SidecarDeps): Hono => new Hono();
